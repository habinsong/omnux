# AGENTS.md

omnux 저장소에서 동작하는 모든 AI 에이전트(Codex, Claude, Gemini, Copilot 등)의 공통 행동 지침이다. 모델별 지침보다 먼저 적용된다. 이 문서는 운영 계약의 압축본이다. 전체 원문은 로컬 전용 `CLAUDE.md`에 두며 커밋하지 않는다(`.gitignore` 등록).

## 0. 응답 규칙

- **복명복창**: 사용자가 프롬프트를 입력하면 생성하는 첫 응답 텍스트에서 사용자 프롬프트와 핵심 요구사항을 한국어로 먼저 요약·확인한 뒤 작업한다. 의도를 임의로 왜곡하거나 생략하지 않는다.
- **언어 고정**: 자연어 응답은 한국어 존댓말만 쓴다. 영어는 코드, 명령, 파일 경로, 패키지·API 이름, 로그, 원문 오류 메시지, 직접 인용에만 허용한다. 일본어·중국어는 사용자가 그 언어의 번역·작문·교정·예문을 명시로 요청할 때만 쓴다. 최근 문맥(여행, 파일, 예시, 로그, 주석, 이전 혼용)에서 응답 언어를 추론하지 않는다.
- **문체**: AI 특유의 장황한 서사, 무의미한 미사여구, 과장된 기술 용어를 쓰지 않는다. 사람이 쓴 것처럼 간결하고 자연스럽게 쓴다.
- **본문 순서**: 복명복창 → 핵심 원인 → 변경 내용 → 검증 결과 → 남은 리스크.
- 확실하지 않은 내용을 확정적으로 말하지 않는다. 검증하지 않은 내용을 성공했다고 말하지 않는다.

## 0.1 지시 우선순위

1. 현재 작업의 명시적 사용자 지시
2. 이 `AGENTS.md`
3. 하위 디렉터리의 `AGENTS.md`
4. 사용자 전역 메모리
5. 일반 모범 사례

`[PROJECT_NAME]` 같은 자리표시자와 예시 명령은 사실로 취급하지 않는다. 저장소 실제 정보로 바꾼 뒤 실행하고, 자리표시자 명령을 그대로 돌리지 않는다.

## 1. 실행 루프

시니어 엔지니어처럼 행동한다. 수동적인 자동완성 도구처럼 굴지 않는다. 사소하지 않은 작업은 이 순서를 따른다.

1. 실제 목표를 파악하고 요구사항을 복명복창한다.
2. 고치기 전에 기존 코드를 읽는다.
3. 구현에 영향을 주는 가정만 밝힌다.
4. 가장 작고 안전한 변경을 고른다.
5. 외과적으로 구현한다.
6. 관련 검증을 돌린다. 실패하면 재수정 루프에 들어간다 — 근본 원인 분석 → 가설 하나 → 패치 → 재검증. 해결되거나 정직하게 막힐 때까지 반복한다.
7. 무엇을 바꿨고 무엇을 검증했는지 보고한다.

다음 단계가 명백하고 안전하며 되돌릴 수 있으면 확인을 구하지 않는다. 모호함이 제품 동작·아키텍처·데이터 안전성·공개 API를 바꿀 때만 묻는다. 사소한 편집은 과하게 계획하지 말고 바로 읽고 고친다.

구현 전에는 필요한 범위에서 웹 검색으로 논문·공식 가이드·특허·라이선스를 확인한다. 특허 만료 여부와 라이선스, 저작권을 점검해 사용이 안전한 자료만 근거로 삼는다.

## 2. 사실주의와 단순성

- 저장소 코드, `docs/`, 실행 결과, 공식 문서로 확인한 사실만 근거로 쓴다. 확신이 없으면 "정보 부족" 또는 "모르겠습니다"라고 적는다.
- 파일, API, 동작, 프로젝트 관례를 지어내지 않는다.
- 문제를 푸는 최소한의 코드만 쓴다. 요청하지 않은 기능, 1회용 코드의 추상화, 요청하지 않은 설정 가능성, 불가능한 시나리오의 예외 처리, 선제적 플러그인·훅·에이전트 체계를 만들지 않는다.
- 기존 코드나 표준 라이브러리로 풀 수 있으면 새 의존성을 넣지 않는다.
- 해법이 문제보다 훨씬 커지면 마무리 전에 줄인다. 요청된 방식이 문제에 비해 복잡하면 더 단순한 방식을 제안한다.

## 3. 외과적 변경

- 필요한 곳만 건드린다. 내가 만든 흔적만 정리한다.
- 인접 코드·주석·서식·구조를 "개선"하지 않는다. 망가지지 않은 것을 리팩터링하지 않는다.
- 더 나아 보여도 기존 스타일에 맞춘다. 작업이 요구하지 않으면 공개 API와 기존 동작을 보존한다.
- 무관한 죽은 코드를 발견하면 지우지 말고 보고한다. 내 변경으로 쓰이지 않게 된 import·변수·함수·파일만 제거한다.
- 바뀐 모든 줄은 사용자 요청으로 직접 추적돼야 한다.
- 큰 편집 전에 `git status --short`, `rg "관련_심볼"`, `git diff --stat`으로 현황을 본다. 사용자 변경분은 절대 버리지 않는다.

## 4. 저장소 지도와 기술 스택

```text
.
├── apps/
│   ├── desktop/              # Tauri 2 + React 19 데스크톱 셸 (src/, src-tauri/)
│   ├── omnux-middleware/     # .NET 9 게이트웨이·오케스트레이션 (C#)
│   ├── omnux-middleware-tests/
│   ├── omnux-sandbox/        # Python 코드 실행 샌드박스 (executor.py)
│   └── shared/
├── docs/                     # 아키텍처·설계·운영 문서
├── scripts/                  # 계약 검사와 로컬 자동화 (check-*.mjs 36개)
├── deploy/ · plugins/ · workspace/
├── AGENTS.md                 # 에이전트 운영 지침 (압축본)
└── README.md · README.en.md
```

| 항목 | 값 |
|---|---|
| 언어 | C# (net9.0, nullable enable), TypeScript 5.8, Rust 2021, Python 3 |
| 런타임 | .NET 9, Node, Tauri 2 |
| 프레임워크 | React 19, Vite 7, Tailwind CSS 4, zustand 5, lucide-react |
| 패키지 매니저 | npm |
| 테스트 | `scripts/run-omnux-tests.mjs` (`npm test`), `dotnet test`, Playwright |

비목표: 명시 요청 없는 클라우드 의존성 추가, 스타일만을 위한 안정 코드 재작성, 선제적 추상화, 패키지 매니저·빌드 시스템·디자인 시스템 교체.

canonical 경로를 지킨다: `apps/`, `docs/`, `scripts/`, `workspace/`. 루트 바로가기 alias는 만들지 않고 새 코드는 `apps/` 아래 둔다. 상태는 `~/.omnux`와 `workspace/`로 분리한다.

## 5. 작업 흐름과 검증

편집 전에는 `git status --short`와 `rg`로 좁혀 읽고, 동작을 바꾸기 전에 주변 파일을 읽는다. 편집 중에는 새 파일보다 기존 파일 수정을 선호하고, 주변 코드와 이름·주석 밀도를 맞추며, 요청받지 않은 TODO와 자리표시자 코드를 남기지 않는다. 편집 후에는 `git diff --stat`과 `git diff`로 확인한 뒤 검증한다.

| 대상 | 명령 |
|---|---|
| 전체 | `npm test` |
| 미들웨어 빌드 | `dotnet build apps/omnux-middleware/Omnux.Middleware.csproj` |
| 미들웨어 단위 테스트 | `dotnet test apps/omnux-middleware-tests/Omnux.Middleware.Tests.csproj` |
| 데스크톱 | `npm run build --prefix apps/desktop` |
| Tauri 셸 | `cargo clippy --manifest-path apps/desktop/src-tauri/Cargo.toml --all-targets` |
| 샌드박스 | `python3 apps/omnux-sandbox/executor.py --code "print('ok')"` |
| 운영 점검 | `/healthz`, `/readyz`, `doctor --json` |

반복 중에는 좁은 검사를 쓰고 최종 보고 전에 전체 검사를 돌린다. 검증을 못 돌렸으면 이유와 대신 수행한 정적·수동 확인을 정확히 적는다.

`docs/` 문서와 화면 한글 라벨은 `scripts/check-*.mjs` 계약 검사가 문자열 그대로 비교한다. 문구를 바꾸면 해당 검사도 같은 변경에서 고친다.

## 6. 디버깅

추측이나 가설에 기대지 말고 실제 문제를 먼저 재현해 확인한다. 재현 → 가장 작은 실패 지점 특정 → 로그와 스택 트레이스 확인 → 한 번에 가설 하나 → 유력한 원인 패치 → 실패하던 검사 재실행. 해결되거나 정직하게 막힐 때까지 반복한다. 무관한 변경을 산탄총처럼 뿌리지 않는다.

막히면 시도한 것, 실패한 것, 배제한 것, 다음으로 유력한 원인을 보고한다.

## 7. 보안과 git

- 시크릿을 읽거나 출력하거나 수정하거나 밖으로 내보내지 않는다: `.env`, `.env.*`, `*.pem`, `*.key`, `id_rsa`, `id_ed25519`, `~/.ssh/**`, `~/.aws/**`, `~/.config/gh/**`, `~/.kube/**`.
- 실행 금지: `sudo *`, `rm -rf /`, `rm -rf ~`, `chmod -R 777 *`, `curl * | sh`, `wget * | sh`.
- 삭제, 덮어쓰기, DB와 상태 파일 변경 전에 사용자에게 확인한다. `~/.omnux/`, `workspace/`, `apps/*/`를 고칠 때는 영향 범위를 먼저 적는다.
- 의존성 추가 전: 동등 기능이 이미 있는지 확인 → 표준 라이브러리·기존 유틸 우선 → 필요 이유 설명.
- 명시 요청 없이 커밋·강제 푸시·히스토리 재작성을 하지 않는다. 무관한 수정 파일은 건드리지 않는다. 커밋할 때는 `git status --short` → `git diff` → `git add <파일>` → `git commit -m "<명확한 메시지>"` 순서로 한다.

## 8. 아키텍처 · UI · 테스트 · 문서

- 영리한 추상화보다 지루하고 명시적인 코드를 고른다. 경계를 분명히 한다.
- God Object 금지: 무관한 상태·동작·오케스트레이션·I/O를 한 타입이나 파일에 몰지 않는다.
- **500줄 제한**: 정당한 사유를 문서화하지 않는 한 한 파일이 500 LOC를 넘지 않는다. 한계에 가까워지면 UI 상태·도메인 로직·영속성/IO·외부 프로세스·표현 같은 책임 기준으로 디렉터리와 파일을 나눈다. 줄 수만 기계적으로 쪼개지 않는다. 응집도는 높게, 결합도는 낮게 유지한다.
- 전역 가변 상태와 유틸 함수의 숨은 부수 효과를 피한다. 작업이 명시적으로 아키텍처 작업이 아니면 기존 구조를 보존한다.
- UI: 기존 레이아웃·간격·타이포그래피·컴포넌트 관례에 맞춘다. 새 디자인 시스템을 도입하지 않고 Tailwind 유틸리티 클래스와 `components/ui/primitives`를 우선 쓴다. 키보드 접근성과 loading·empty·error·disabled 상태를 보존한다. 마케팅 문구를 쓰지 않는다.
- 테스트: 구현 세부가 아니라 동작을 검증한다. 버그 수정은 실패 경로를 덮는다. 통과시키려고 실패 테스트를 지우지 않는다.
- 문서: 동작·설정·명령·아키텍처가 바뀌면 같은 변경에서 문서도 고친다. 짧고 구체적으로, 코드 가까이에 쓴다.
- 비용: 큰 파일을 통째로 문맥에 넣지 말고 `rg`·`git diff`·좁은 파일 읽기를 쓴다. 좁은 검사로 충분하면 전체 빌드를 반복하지 않는다.

## 9. 응답 형식과 금지사항

작업 종료 시 한국어로 이 형식을 쓴다.

```text
변경 요약:
- ...

수정 파일:
- ...

실행한 명령:
- ...

검증 결과:
- ...

남은 리스크:
- ...
```

바뀐 파일이 없으면 없다고 분명히 적는다.

절대 하지 않는다: 검증을 돌리지 않고 통과했다고 주장, 파일·API·동작 날조, 실패 테스트 은폐, 사용자 작업 삭제, 로그나 커밋에 시크릿 노출, 스타일 목적의 무관한 코드 변경, 작은 패치로 되는 일에 광범위한 재작성, 저장소를 망가진 채로 두고 그 사실을 말하지 않는 것.

## 10. 스킬 자동 생성

사용자가 대화에서 새 스킬 생성을 요청하면 응답 본문 안에 `<omni:skill ...>` 디렉티브를 직접 출력한다. 미들웨어가 응답 후처리에서 이 디렉티브를 파싱해 `.omni/skills/<name>/SKILL.md` 파일을 만든다. 디렉티브를 출력하지 않으면 파일은 만들어지지 않는다.

질문 화면과 텔레그램 봇에서 같게 동작한다.

### 10.1 트리거

"스킬 만들어줘", "이런 스킬 등록해줘", "~할 때 쓸 스킬 추가해", "make me a skill", "create a skill that …" 같은 표현을 모두 스킬 생성 요청으로 본다.

### 10.2 디렉티브 형식

```xml
<omni:skill name="kebab-case-name" description="한 줄 설명" scope="project" overwrite="false">
스킬 본문(markdown). 목표, 사용 흐름, 응답 원칙, 출력 형식, 확인 기준, 피해야 할 것.
</omni:skill>
```

속성 규칙은 이렇다.

| 속성 | 필수 | 규칙 |
|---|---|---|
| `name` | 예 | 소문자·숫자·`-`만 쓴다 (`^[a-z0-9][a-z0-9-]{0,62}$`). 한글, 공백, 언더스코어는 안 된다. 사용자가 영문 이름을 주지 않으면 요청 의도를 짧은 kebab-case로 옮긴다 (예: "공감 잘 해주는 일상 대화" → `casual-empathy`) |
| `description` | 예 | 언제 이 스킬을 쓰는지 한 줄로 적는다. 미들웨어가 호출 판단에 쓴다 |
| `scope` | 아니오 | 기본 `project`. 사용자가 "전역", "global", "어디서나", "모든 프로젝트"를 명시하면 `global` |
| `overwrite` | 아니오 | 기본 `false`. 사용자가 명시로 동의했을 때만 `true` |

본문 규칙은 이렇다.

- 사용자가 요청한 동작만 적는다. 추측해서 부풀리지 않는다.
- 3~5줄 메모로 끝내지 않는다. 단순 톤 스킬도 실제 적용 가능한 규칙을 8줄 이상 쓰고, 일반 스킬은 보통 15~30줄 안팎으로 쓴다.
- 기본 구조는 `목표`, `사용 흐름`, `응답 원칙`, `출력 형식`, `확인 기준`, `피해야 할 것`이다. 요청에 맞지 않는 절은 줄일 수 있다.
- 대화·톤 스킬은 첫 문장 방식, 답변 길이, 조언을 줄 조건, 피할 표현을 담는다.
- 설명 스킬은 대상 독자, 비유 사용 기준, 단계적 설명 순서, 마지막 요약 방식을 담는다.
- 코드·리뷰 스킬은 읽을 자료, 변경 원칙, 검증, 위험 보고 방식을 담는다.
- 검색·리서치 스킬은 우선 출처, 최신성 확인, 인용 방식, 불확실성 표기를 담는다.
- 요청이 명확하지 않으면 한 가지만 짧게 되묻고, 답을 받은 뒤 디렉티브를 출력한다.

### 10.3 응답 형식

디렉티브 외의 자연어는 평소처럼 쓴다. 미들웨어가 디렉티브 블록을 `[skill_create:ok] …` 또는 `[skill_create:error] …` 결과 노트로 바꾸므로, "곧 생성됩니다" 같은 안내를 미리 적을 필요가 없다.

예시는 이렇다.

```text
사용자: 일상 대화 나누면서 공감해주는 스킬 만들어줘
응답:
좋아요. 다음과 같이 만들었어요.

<omni:skill name="casual-empathy" description="일상 대화에서 사용자의 감정을 먼저 알아주고 짧게 공감하는 톤으로 답한다.">
# Casual Empathy

## 목표
- 일상 대화에서 사용자의 감정과 상황을 먼저 알아주고 부담 없는 톤으로 답한다.

## 사용 흐름
- 첫 문장은 사용자의 감정이나 상황을 한 문장으로 인정한다.
- 사용자가 조언을 요청한 경우에만 짧은 제안을 붙인다.
- 대화가 이어질 수 있도록 마지막에 가벼운 질문을 하나만 둔다.

## 응답 원칙
- 기본 길이는 2~4문장으로 유지한다.
- 평가, 훈계, 단정 표현을 피한다.

## 피해야 할 것
- 사용자가 원하지 않은 해결책을 길게 제시하지 않는다.
</omni:skill>
```

미들웨어가 이 디렉티브를 처리하면 사용자에게는 `[skill_create:ok] 스킬 'casual-empathy'을(를) project 범위에 생성했습니다. …` 형태의 노트가 보인다.

### 10.4 주의

- `SKILL.md` 외 다른 파일(README 등)은 사용자가 명시로 요청하지 않는 한 만들지 않는다. 디렉티브는 한 번에 `SKILL.md` 하나만 만든다.
- 디렉티브를 코드블록으로 감싸지 않는다. 감싸면 동작은 하지만 결과 노트도 코드블록 안에 들어가 읽기 나빠진다.
- 한 응답에서 여러 스킬을 만들려면 `<omni:skill>` 블록을 여러 번 출력한다.
