# 질문탭(Ask) UI/UX 리디자인 계획

작성 기준: 2026-06-06

## 1. 현재 화면의 핵심 문제

| # | 문제 | 심각도 |
|---|---|---|
| 1 | **1774줄 단일 파일** — 15개 컴포넌트가 한 파일에 몰려 유지보수 불가 | 구조적 |
| 2 | **상단 툴바 과적** — 배지 6개 + 모드 선택 + 모델 선택 + 요약 선택 + 버튼 10개가 flex-wrap으로 깔려 있음. 스캔 불가 | 사용성 |
| 3 | **Sparkles 아이콘 남발** — Think+ 토글, 워커 모델, 모델 선택, ModelDock 헤더 등 5곳에서 사용 | AI SaaS |
| 4 | **BrainCircuit 아이콘 남발** — 압축 생성, 메모리 노트, RAG 점검 등 4곳 | AI SaaS |
| 5 | **설명문/카피라이팅** — "대화, 메모리, 모델 라우팅을 한 화면에서 확인합니다.", "single·orchestration·multi 모드로 모델 라우팅을 비교할 수 있습니다." | AI SaaS |
| 6 | **사이드 패널이 인라인** — 정보/메모리/모델/문맥 패널이 대화 영역 너비를 침범 | 레이아웃 |
| 7 | **메시지 액션 7개 상시 노출** — NB, PL, 빌드, 루틴, 비교, 읽기, 복사가 매 응답마다 항상 보임 | 밀도 |
| 8 | **보관함 하단 메모리 프리뷰** — 3개만 보여주고 끊김. 본체는 사이드 패널에 있는데 중복 | 중복 |
| 9 | **Suggested prompts** — Empty state에 "요약", "코드 리뷰", "모델 비교", "파일 분석" pill 버튼이 랜딩페이지 느낌 | AI SaaS |
| 10 | **하단 입력 바 아이콘 6개 + 상태 배지** — 음성, 첨부, Think+, 루틴, 작업계획 + "대기"/"Think+ 켜짐" 배지 | 밀도 |

## 2. 유지할 요소

- 2-column 레이아웃 (보관함 340px + 대화)
- 대화 모드 전환 Single/Orch/Multi
- 폴더 그룹핑 + 검색
- RAG preflight + Vision
- 메모리 dock 기능
- 첨부/드래그앤드롭
- 음성 입력
- 메시지 액션 (NB, PL, 빌드, 루틴, 비교)
- WorkerModelStrip (Orch/Multi 모델 설정)
- Multi result 뷰어 (요약 + provider별 탭)
- 모바일 pane 전환

## 3. 바꿀 요소

| 현재 | 변경 |
|---|---|
| Sparkles 아이콘 5곳 | Think+에만 `Zap` 사용. 모델/워커는 `Settings2` 또는 텍스트만 |
| BrainCircuit 아이콘 4곳 | 전부 교체. 압축은 `Minimize2`, RAG는 `Search`, 메모리는 `BookOpen` |
| 상단 툴바 15~18개 요소 flex-wrap | **2-tier 툴바**: 1차(모드+모델+상태) + 2차(패널 토글은 오른쪽 아이콘 그룹) |
| 사이드 패널 인라인 확장 | **오른쪽 슬라이드 패널** — 대화 영역 너비 침범 안 함 |
| 메시지 액션 7개 상시 노출 | 복사만 기본. 나머지는 `...` 더보기 드롭다운 |
| 보관함 하단 메모리 3개 | 제거. 메모리는 사이드 패널에서만 |
| Empty state에 suggested prompts | "대화 없음" + [새 대화] 버튼만 |
| 설명문 2줄 | "질문" 한 줄만 |
| 하단 입력 바 아이콘 6개 + 배지 | 음성, 첨부, Think+, 전송만. 나머지는 `+` 더보기 메뉴 |
| "대기" / "Think+ 켜짐" 상태 배지 | Think+ 토글 버튼 자체에 active 스타일로 표시. 배지 제거 |

## 4. 레이아웃 수정안

```
┌─────────────────────────────────────────────────────────┐
│ 질문                                                     │
├────────────┬────────────────────────────┬───────────────┤
│ 보관함      │ 대화 본문                   │ 사이드 패널    │
│ (300px)     │                            │ (280px,       │
│             │ ┌─ 1차 툴바 ─────────────┐ │  필요시만)    │
│ [새 대화]   │ │ [S][O][M] [Groq▼]      │ │              │
│ 🔍 검색     │ │ [model▼]    ● 생성 중   │ │ 정보/메모리/  │
│             │ └────────────────────────┘ │ 모델/문맥     │
│ 📁 폴더1   │                            │              │
│   대화1 ●  │ [사용자]                    │              │
│   대화2    │                             │              │
│ 📁 폴더2   │ [AI 응답]                  │              │
│   대화3    │   [복사] [...]              │              │
│             │                            │              │
│             │ ── RAG/Vision (있으면) ──  │              │
│             │                            │              │
│             │ ┌─ 2차 툴바(오른쪽) ─────┐ │              │
│             │ │ [ℹ][📦][⚙][📋]        │ │              │
│             │ └────────────────────────┘ │              │
│             │                            │              │
│             │ ┌─ 입력 ─────────────────┐ │              │
│             │ │ [Textarea]              │ │              │
│             │ │ [🎤][📎][⚡][⋯]  [전송] │ │              │
│             │ └────────────────────────┘ │              │
└────────────┴────────────────────────────┴───────────────┘
```

핵심 변화:

- 보관함 340px → 300px (더 compact)
- 사이드 패널이 대화 본문 밖에 있음 (3-column when open, 2-column when closed)
- 2차 툴바가 대화 본문 오른쪽 상단에 세로 배치 (또는 가로 1줄이지만 주요 액션과 분리)
- 메시지 액션: 복사 + "..." 만

## 5. 아이콘 수정안

| 현재 | 변경 | 이유 |
|---|---|---|
| `Sparkles` (Think+) | `Zap` | Sparkles = AI 마법. Zap = 토글/부스트 |
| `Sparkles` (워커 모델) | `Settings2` 또는 제거 | 설정 아이콘으로 |
| `Sparkles` (ModelDock 헤더) | `SlidersHorizontal` | 모델 설정 |
| `BrainCircuit` (압축 생성) | `Minimize2` | 압축 = 줄이기 |
| `BrainCircuit` (RAG 점검) | `Search` | 검색 = 검색 |
| `BrainCircuit` (메모리 노트) | `BookOpen` | 메모리 = 책 |
| `BrainCircuit` (하단 루틴 저장) | `CalendarPlus` 유지 | 루틴 = 이미 적절 |
| `BrainCircuit` (ModelDock 워커) | `Users` 또는 `Layers` | 워커 = 다중 |

## 6. 문구 수정안

| 현재 | 변경 |
|---|---|
| "대화, 메모리, 모델 라우팅을 한 화면에서 확인합니다." | **삭제** |
| "메시지를 입력해 대화를 시작하세요" | "대화 없음" |
| "single·orchestration·multi 모드로 모델 라우팅을 비교할 수 있습니다." | **삭제** |
| "메시지 보관함 대화 검색" | "검색" |
| "검색 점검" | "RAG" |
| "Vision 점검" | "Vision" |
| "이 답변으로 작업계획 만들기" | "계획" (tooltip만 길게) |
| "노트북에 저장" | "저장" (tooltip만 길게) |
| "생성 중" | Spinner만 (텍스트 제거) |
| "대기" 배지 | **삭제** (불필요한 상태 표시) |
| "Think+ 켜짐" 배지 | **삭제** (버튼 active 스타일로 충분) |
| "세션 {id}" 배지 | **삭제** (세션 ID는 사이드 패널 정보 탭으로) |
| "질문" (h1) | 유지 |

## 7. 컴포넌트 밀도와 간격

| 요소 | 현재 | 변경 |
|---|---|---|
| 대화 목록 행 | 제목+프리뷰+시간+카테고리+태그+hover 5개 버튼 | 제목+시간+카테고리. 프리뷰 hover. 액션은 우클릭 또는 `...` |
| 상단 툴바 | 15~18개 요소 flex-wrap | 1차 6~7개 + 2차 아이콘 4개 |
| 메시지 액션 | 7개 버튼 항상 노출 | 복사 1개 + `...` 드롭다운 6개 |
| 메시지 말풍이 | 배지 6개 (source, provider, model, route, web, meta) | provider+model만 기본. 나머지는 hover |
| 하단 입력 바 | 6개 아이콘 + 배지 + 전송 | 4개 아이콘 + 전송. 나머지 `⋯` 메뉴 |
| 사이드 패널 | 대화 영역 안에 인라인 확장 | 별도 column (열 때만 3-column) |

## 8. Light / Dark / Glass 대응

| 모드 | 현재 문제 | 변경 |
|---|---|---|
| Light | 배경이 `bg-muted/20`, `bg-muted/25`, `bg-muted/30`, `bg-muted/40` 등 다양해서 표면 위계가 불명확 | 3단계만: background → card → muted. 비율 고정 |
| Dark | 같은 문제. `bg-card/60`, `bg-card/70` 등 반투명이 많아 경계 모호 | 배경/카드/뮤트드 3단계. border로 경계 명확 |
| Glass | backdrop-blur가 여러 곳에 인라인으로 들어가 있음 | Glass 테마에서만 blur 적용. Light/Dark는 blur 없음 |

## 9. 파일 분할 계획

AskPage.tsx (1774줄) → 8개 파일로 분할:

| 파일 | 컴포넌트 | 줄수 (추정) |
|---|---|---|
| `AskPage.tsx` | `AskPage` (레이아웃만) | ~120 |
| `ConversationList.tsx` | 보관함 전체 (검색, 폴더, 대화 항목, 선택 모드) | ~250 |
| `MessageThread.tsx` | 메시지 목록, 말풍이, Multi 결과 | ~250 |
| `ComposerBar.tsx` | 입력창, 하단 툴바, 첨부 패널, 음성 | ~200 |
| `ChatToolbar.tsx` | 상단 툴바 (모드, 모델, 상태 배지) | ~150 |
| `MessageActions.tsx` | 메시지별 액션 (복사 + 더보기) | ~80 |
| `AskSidePanel.tsx` | 정보/메모리/모델/문맥 패널 | ~250 |
| `AskShared.tsx` | `ChoiceMenu`, `formatBytes`, `formatTime` 등 공유 유틸 | ~100 |

기존 파일 유지: `ask-store.ts`, `ask-context.ts`, `ask-models.ts`, `ask-rag.ts`, `ask-speech.ts`, `ask-vision.ts`, `AskVisionPanel.tsx`, `MarkdownMessage.tsx`

## 10. 구현 시 주의할 디테일

1. **사이드 패널 column 전환** — 패널 열리면 `grid-cols-[300px_minmax(0,1fr)_280px]`, 닫히면 `grid-cols-[300px_minmax(0,1fr)]`. CSS transition으로 너비 변화 부드럽게.

2. **메시지 액션 더보기** — `...` 버튼 클릭 시 작은 popover에 NB, PL, 빌드, 루틴, 비교, 읽기 나열. 복사는 항상 보이게.

3. **2차 툴바** — 정보/메모리/모델/문맥 토글을 대화 영역 오른쪽 상단에 아이콘 4개로 배치. active면 primary 스타일.

4. **하단 `⋯` 메뉴** — 루틴으로 저장, 작업계획 만들기를 popover로. 자주 안 쓰는 액션.

5. **RAG/Vision 패널 위치** — 메시지 목록 안에 인라인으로 유지하되, 패널 디자인을 사이드 패널과 통일.

6. **Empty state** — `Send` 아이콘 + "대화 없음" + [새 대화] 버튼. Suggested prompts 제거.

7. **WorkerModelStrip** — Orch/Multi 모드에서만 상단 툴바 아래에 표시. 현재 디자인 유지하되 Sparkles → `SlidersHorizontal`.

8. **접근성** — 현재 `aria-label`이 잘 되어 있음. 분할 후에도 유지.

## 11. 최종 검수 질문

- 이 화면의 주 작업이 바로 보이는가? → **대화 입력과 메시지 열람이 중앙에 있음. Yes.**
- 버튼과 메뉴명이 짧고 자연스러운가? → **"새 대화", "전송", "RAG", "Vision" 등 짧게 정리. Yes.**
- 아이콘이 기능을 설명하는가? → **Sparkles/BrainCircuit 제거 후 기능 매칭 개선. Yes.**
- 장식이 기능보다 앞서지 않는가? → **설명문, 배지, suggested prompts 제거. Yes.**
- AI SaaS 템플릿처럼 보이지 않는가? → **카피라이팅, Sparkles, empty state 랜딩 페이지 제거. Yes.**
- 실제 사람이 오래 쓸 수 있는 화면인가? → **툴바 정리, 사이드 패널 분리, 메시지 액션 정리로 밀도 개선. Yes.**
- Light Mode에서 가장 안정적으로 보이는가? → **표면 위계 3단계로 통일. Yes.**
- Dark Mode에서도 대비가 충분한가? → **border로 경계 명확. 반투명 제거. Yes.**
- Glass Mode에서도 가독성이 무너지지 않는가? → **blur 절제, 가독성 우선. Yes.**
- 빈 공간이 미완성처럼 보이지 않는가? → **empty state에 명확한 CTA. Yes.**
- 텍스트가 줄바꿈으로 깨지지 않는가? → **버튼 텍스트 짧게 유지. Yes.**
- 자주 쓰는 액션과 위험 액션이 명확히 구분되는가? → **삭제는 destructive 스타일 유지. 복사는 기본, 나머지는 더보기. Yes.**
