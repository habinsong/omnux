# 결함 #4 (CommandService God Object) — M4·M5 완전 해결 가이드

> 이 문서는 결함 #4 탈결합 작업의 **M4(무거운 도메인 이관)** 와 **M5(레거시 제거·결합도 재측정)** 를
> 다른 AI 에이전트나 개발자가 맥락 없이도 이어서 작업할 수 있도록, **문제 진단부터 단계별 해결 방법까지** 빠짐없이 정리한다.
>
> - 최종 작성: 2026-06-03
> - 대상 코드: `apps/omnux-middleware` (.NET 9, `PublishAot=true`)
> - 상위 설계: `~/.claude/plans/expressive-finding-fox.md` (M1~M5 로드맵 원본)
> - 상위 현황: [`develop.md`](develop.md) 의 결함 #4 항목 (현재 "1차 보강, M4 완료·M5 안전 레거시 제거와 gateway adapter 경계 완료, routine logic graph/search gateway/model/rate-limit 1차 분리, 심층 상태 분리 잔여")
>
> **한 줄 결론: M5-lite, M4-R routine 이관, M4-C coding 이관, M5 안전 레거시 제거, gateway adapter 경계, routine logic graph/search gateway/model/rate-limit 1차 분리까지 완료했다.**
> `/llm`·채널·routine은 진짜 application service/handler로 빠졌고, coding도 `_inner` 래퍼가 아닌
> `CodingApplicationService*.cs` 실제 구현과 `CodingSlashCommandHandler`로 이동했다. 추가로 core runtime slash handler,
> 텔레그램 wrapper partial 제거, routine app service의 `CommandService.*` 정적 bridge 제거, gateway 인터페이스 직접 구현 제거까지 끝냈다.
> **이번 회차에는 심층 분리의 첫 진짜 슬라이스로 URL context answer를 단일 소스 `GeminiUrlContextAnswerService`로 수렴했다**:
> chat/telegram(`SearchPipeline`)과 routine이 같은 서비스에 위임하고(중복 ~190줄 제거), 서비스는 `IGeminiUrlContextLlm` 인터페이스에만
> 의존해 concrete `LlmRouter` 없이 fake로 단독 단위 테스트된다(테스트 2개 추가, 전체 1125).
> 남은 핵심은 **gateway adapter 내부가 호출하는 나머지 공유 chat 엔진 상태(grounded web composition·input 준비·citation)의 실제 service 분리**다.

---

## 0. TL;DR (의사결정용)

| 질문 | 답 |
|---|---|
| M4+M5 한방 가능? | M4와 M5 안전 레거시 제거, gateway adapter 경계, routine logic graph/search gateway/model/rate-limit 1차 분리는 완료. 심층 상태 service 분리는 후속 대형 단계로 남음. |
| M4에서 이미 끝난 것 | `/llm`(LlmControl), 채널 설정, **routine 오케스트레이션 + routine slash handler**, **coding 오케스트레이션 + coding slash handler**, core runtime slash handler 완료. |
| M4에 남은 것 | 없음. 텔레그램 `/coding` direct 경로는 모바일 파일 UX 보존용으로 남아 있고, WebSocket coding은 `ICodingApplicationService`를 직접 쓴다. |
| 왜 무거웠나 | `CommandService.Coding*.cs` 8,405줄과 `CommandService.Utils.cs`의 coding loop 1,345줄이 `Application/CodingApplicationService*.cs` 10,319줄 실제 구현으로 이동했다. |
| M5에서 추가로 끝낸 것 | `CommandService.RoutineCommands.cs`, 텔레그램 doctor/plan/task/notebook/memory wrapper partial, post-routing `/metrics`·`/kill` 직접 처리, kill guard helper, routine app service의 `CommandService.*` 정적 bridge 제거, `CommandService : ICodingCommandGateway/IRoutineLlmGateway` 직접 구현 제거, routine logic graph/search gateway/model/rate-limit 분리. |
| 아직 남은 이유 | adapter 내부가 아직 grounded web composition·입력 준비·citation 공통 상태를 `CommandService`에서 호출한다. **URL context answer는 이번에 `GeminiUrlContextAnswerService`(단일 소스, `IGeminiUrlContextLlm` 의존, 단위 테스트 2개)로 분리 완료** — 나머지는 같은 패턴으로 순차 분리. |
| 한 세션에 안전하게 끝낸 것 | **옵션 A: M5-lite** + **옵션 B: M4-R** + **옵션 C: M4-C coding 이관** + **M5 안전 레거시 제거** + **gateway adapter 경계** + **routine logic graph/search gateway/model/rate-limit 1차 분리** + **URL context answer 단일 소스 수렴(+서비스 단위 테스트)**. |
| 추천 순서 | URL context answer와 동일 패턴(좁은 LLM 인터페이스 + 단일 service + 단위 테스트)으로 grounded web composition → input 준비 → citation service를 순차 이전. |

---

## 1. 배경 — 결함 #4란 무엇인가

`CommandService` 는 웹 대시보드와 텔레그램 봇의 모든 명령이 통과하는 미들웨어의 척추다. 시간이 지나며
하나의 `partial class` 가 거대해졌다.

**현재 측정치 (2026-06-03, `apps/omnux-middleware/src` 기준):**

| 지표 | 값 | 측정 명령 |
|---|---|---|
| `CommandService*.cs` partial 파일 수 | **52** | `find apps/omnux-middleware/src -maxdepth 1 -name 'CommandService*.cs' -print \| wc -l` |
| 총 줄 수 | **24,378** | `find apps/omnux-middleware/src -maxdepth 1 -name 'CommandService*.cs' -print0 \| xargs -0 wc -l` |
| 구분되는 private 필드 수 | **82** | `find apps/omnux-middleware/src -maxdepth 1 -name 'CommandService*.cs' -print0 \| xargs -0 grep -hoE "private [A-Za-z0-9_<>,?. ]+ _[a-zA-Z0-9]+" \| grep -oE "_[a-zA-Z0-9]+$" \| sort -u \| wc -l` |

기준선(2026-06-02, M5-lite/M4-R/M4-C 전)은 79파일, 38,743줄, private 필드 91개였다.

`develop.md` 의 `3.6 팩트폭행 리뷰` 가 스스로 진단한 핵심:

> "Partial 클래스와 Policy로 나눈 것은 코드를 **물리적으로 분산**시켰을 뿐, 아키텍처의 **논리적 결합도는
> 낮추지 못했다**. (= 가짜 리팩터링)"

즉 파일만 쪼갰을 뿐, 80개 partial이 동일한 91개 공유 가변 private 필드(`_conversationStore`, `_context`,
`_auditLogger`, `_llmRouter`, `_codeRunner`, `_runArtifactStore`, `_routineSchedulerTask` …)를 직접 만진다.
한 곳을 고치면 다른 곳이 깨지는 진짜 God Object다.

### "완전 해결" 의 정의

라우터가 명령을 도메인 핸들러에 위임하고, **각 핸들러가 CommandService private state가 아니라 자기 도메인
ApplicationService(진짜 구현)만 의존**하도록 dispatch 경계를 세워 91필드 결합을 **실제로** 줄이는 것.
파일을 더 쪼개는 것이 아니라, **상태 의존을 끊는 것**이 목표다.

---

## 2. 채택한 아키텍처 — strangler-fig + AOT-safe 수동 라우터

### 2.1 왜 이 패턴인가

- 이 코드베이스에는 이미 정답 패턴이 있다: **WebSocket 측 `Ws*CommandDispatcher` 12개**는
  `internal sealed`, 생성자로 ApplicationService 주입, `TryHandleAsync → bool`, CommandService 미의존 형태로
  이미 분리돼 있다. (단, 2.4의 함정 주의)
- `Omnux.Middleware.csproj` 는 **`PublishAot=true`** → 리플렉션 기반 MediatR/DI 컨테이너는 **비호환**.
  따라서 새 프레임워크를 도입하지 않고, 검증된 Ws dispatcher 패턴을 **텍스트/슬래시 명령 경로에 그대로** 적용한다.
- **strangler-fig(교살자 무화과):** 도메인을 하나씩 새 핸들러로 떼어내고, 라우터가 "소유 핸들러가 없으면
  `null` → 레거시 경로로 fall-through" 하게 해서 회귀 없이 점진 이관한다. 마지막에 레거시 경로와 그때
  쓰이지 않게 된 private 필드를 제거한다(= M5).

### 2.2 라우팅 골격

```
ExecuteNormalizedCommandRoutingAsync (라우터, 얇음)
  → (텔레그램 direct 보존 경로 먼저)
  → SlashCommandRouter.TryHandleAsync(ctx)        // 등록 핸들러를 순서대로 consult
      → StaticSlashCommandHandler
      → CoreRuntimeSlashCommandHandler (/metrics, /kill)
      → DoctorSlashCommandHandler   (의존: IDoctorApplicationService)
      → NotebookSlashCommandHandler (의존: INotebookApplicationService)
      → HandoffSlashCommandHandler / PlanSlashCommandHandler / TaskSlashCommandHandler
      → MemorySlashCommandHandler / ChannelSettingsSlashCommandHandler / LlmControlSlashCommandHandler
      → RoutineSlashCommandHandler / CodingSlashCommandHandler
  → (miss = null) 비슬래시 자연어/텔레그램 chat/intent fallback
```

### 2.3 코드 상의 핵심 지점 (인용)

**핸들러 수동 등록 (AOT-safe, 리플렉션 없음)** — [`Program.cs`](apps/omnux-middleware/src/Program.cs)

```csharp
var slashCommandHandlers = new List<ISlashCommandHandler>
{
    new StaticSlashCommandHandler(),
    new DoctorSlashCommandHandler(appServices.Doctor),
    new NotebookSlashCommandHandler(appServices.Notebook),
    new HandoffSlashCommandHandler(appServices.Notebook),
    new PlanSlashCommandHandler(appServices.Plan),
    new TaskSlashCommandHandler(appServices.TaskGraph),
    new MemorySlashCommandHandler(appServices.Memory, appServices.Conversation),
    new ChannelSettingsSlashCommandHandler(llmSettingsApplicationService),
    new LlmControlSlashCommandHandler(llmControlApplicationService),
};
var slashCommandRouter = new SlashCommandRouter(slashCommandHandlers);
// commandService 생성 후:
slashCommandHandlers.Add(new CoreRuntimeSlashCommandHandler(coreClient, auditLogger, config.Security.KillAllowlistCsv, commandService.RecordRoutedEvent));
// routine service 생성 후:
slashCommandHandlers.Add(new RoutineSlashCommandHandler(routineApplicationService));
// coding service 생성 후:
slashCommandHandlers.Add(new CodingSlashCommandHandler(codingApplicationService));
```

**웹 경로 strangler-fig 와이어인** — [`CommandService.Execution.Dispatch.cs:96`](apps/omnux-middleware/src/CommandService.Execution.Dispatch.cs)

```csharp
// 텔레그램 direct(텔레그램 전용 동작 보존) 다음, post-routing fallback 앞에서 consult한다.
var slashRouterResult = await TryHandleViaSlashRouterAsync(text, source, cancellationToken);
if (slashRouterResult != null)
{
    return slashRouterResult;   // 소유 핸들러가 처리
}
return await ExecutePostUnifiedRoutingAsync(...);   // miss → 자연어/telegram chat/intent fallback
```

**텔레그램 경로도 동일 라우터로 위임** — 현재는 doctor/plan/task/notebook/memory 전용 wrapper partial 없이
`CommandService.Execution.Dispatch.cs`의 공통 slash router 또는 텔레그램 pseudo-command handler map의 `slashRouter` delegate를 탄다.

```csharp
TelegramPseudoCommandHandler slashRouter =
    (command, token) => TryHandleViaSlashRouterAsync(command, "telegram", token);
```

> `CommandService.Telegram.Doctor.cs`, `CommandService.Telegram.Notebooks.cs`, `CommandService.Telegram.Plans.cs`,
> `CommandService.Telegram.Tasks.cs`, `CommandService.Telegram.MemoryCommand.cs`는 M5 안전 정리에서 삭제했다.

### 2.4 ⚠️ 핵심 함정 — "인터페이스 탈결합 ≠ 진짜 탈결합"

`Ws*CommandDispatcher` 가 `IRoutineApplicationService`/`ICodingApplicationService` 를 의존한다고 해서 자동으로 탈결합된 것이 **아니다.**
그 인터페이스의 **구현체가 CommandService로 되돌아가는 위임 래퍼**이면, 오케스트레이션은 여전히 God Object 안에 있다.

과거 [`Application/RoutineApplicationService.cs`](apps/omnux-middleware/src/Application/RoutineApplicationService.cs) 는 이런 형태였다:

```csharp
public sealed class RoutineApplicationService : IRoutineApplicationService
{
    private readonly CommandService _inner;                       // ← God Object로 되돌림
    public RoutineApplicationService(CommandService inner) { _inner = inner; }

    public IReadOnlyList<RoutineSummary> ListRoutines() => _inner.ListRoutines();
    public Task<RoutineActionResult> CreateRoutineAsync(...) => _inner.CreateRoutineAsync(...);
    // ... 모든 메서드가 _inner.* 로 전달만 한다
}
```

과거 [`Application/CodingApplicationService.cs`](apps/omnux-middleware/src/Application/CodingApplicationService.cs) 도 동일하게
`_inner.RunCodingSingleAsync(...)` 처럼 전부 전달만 했다. 현재는 `RoutineApplicationService`와
`CodingApplicationService` 모두 `_inner` 래퍼가 아니며, 생성자 주입과 명시적 gateway bridge로 실제 구현을 소유한다.

**이 함정 제거가 M4의 본질이다.** routine/coding 같은 대형 도메인을 진짜 탈결합하려면 위임 방향을 **뒤집어야** 한다:
구현(오케스트레이션)을 CommandService 밖으로 빼서 application service가 **소유**하게 하고, CommandService가
(필요하면) 그 service에 위임하거나 아예 그 코드를 들어내야 한다.

---

## 3. 현재 진행 상태 — 무엇이 끝났고 무엇이 남았나

### 3.1 이관 완료 핸들러 (12종, 와이어됨)

| 핸들러 | 명령 | 의존 application service | service 상태 |
|---|---|---|---|
| `StaticSlashCommandHandler` | 정적 help/usage 텍스트 | (순수, 없음) | — |
| `CoreRuntimeSlashCommandHandler` | `/metrics`, `/kill <pid>` | `ICoreRuntimeClient` + kill guard policy | ✅ CommandService 없이 단독 테스트 |
| `DoctorSlashCommandHandler` | `/doctor` | `IDoctorApplicationService` | ✅ 진짜 구현 134줄 |
| `NotebookSlashCommandHandler` | `/notebook` | `INotebookApplicationService` | ✅ 진짜 구현 51줄 |
| `HandoffSlashCommandHandler` | `/handoff` | `INotebookApplicationService` | ✅ 진짜 구현 |
| `PlanSlashCommandHandler` | `/plan` | `IPlanApplicationService` | ✅ 진짜 구현 443줄 |
| `TaskSlashCommandHandler` | `/task` | `ITaskGraphApplicationService` | ✅ 진짜 구현 114줄 |
| `MemorySlashCommandHandler` | `/memory` | `IMemoryApplicationService` + `IConversationApplicationService` | ✅ 진짜 구현 193 / 1420줄 |
| `ChannelSettingsSlashCommandHandler` | `/talk /code /profile /mode /provider /model /status model` | `ILlmSettingsApplicationService` | ✅ 진짜 구현 542줄 |
| `LlmControlSlashCommandHandler` | `/llm` (help/usage/models/set …) | `ILlmControlApplicationService` | ✅ 진짜 구현 331줄 |
| `RoutineSlashCommandHandler` | `/routine /routines` | `IRoutineApplicationService` | ✅ 진짜 구현 5,097줄 |
| `CodingSlashCommandHandler` | `/coding run`, `/coding single/orchestration/multi run` | `ICodingApplicationService` | ✅ 진짜 구현 10,319줄 |

핸들러 격리 테스트: `apps/omnux-middleware-tests/*SlashCommandHandlerTests.cs` — 각 핸들러를 **CommandService 없이
fake application service만으로** 단독 생성·검증한다. 이 테스트의 존재 자체가 "God Object 없이 동작한다"는 탈결합 증명이다.

### 3.2 도메인 이관 상태

| 도메인 | app service 상태 | 실제 오케스트레이션 위치 | 줄 수 |
|---|---|---|---|
| **routine** | ✅ 실제 구현 (`_inner` 제거) | `Application/RoutineApplicationService*.cs` + `CommandDispatch/RoutineSlashCommandHandler.cs` | **5,409** |
| **coding** | ✅ 실제 구현 (`_inner` 제거) | `Application/CodingApplicationService*.cs` | **10,319** |
| **coding slash** | ✅ 실제 핸들러 | `CommandDispatch/CodingSlashCommandHandler.cs` | **214** |
| | | **M4 잔여** | **없음** |

이제 routine과 coding app service는 모두 `_inner` 없는 진짜 구현이며, 텍스트 slash 명령도 `CommandDispatch`
핸들러가 소유한다.

**routine 이관 결과**:
`Application/RoutineApplicationService*.cs` 5,097줄과 `CommandDispatch/RoutineSlashCommandHandler.cs` 312줄이
루틴 오케스트레이션과 slash 명령을 소유한다. `CommandService.RoutineCommands.cs`는 삭제됐고,
routine app service가 `CommandService.*` 정적 helper를 다시 호출하던 `CommandBridge`도 자체 순수 helper로 전환했다.
`CommandService.RoutineGateway.cs`는 `RoutineLlmGatewayAdapter`로 직접 인터페이스 구현을 끊었다. 또한 dynamic-code flag,
workspace root, URL 해석, grounded web 실패 판정, 텔레그램 응답 포맷팅은 `RoutineApplicationService`가 직접 소유하게 했다.
이번 추가 회차에는 `IRoutineLlmGateway`에서 logic graph 실행을 빼고 `IRoutineLogicGraphRunner`와
`CommandService.RoutineLogicGraphRunner.cs`로 별도 bridge를 만들었다. 이어서 search/url 모델 해석과 Groq rate-limit 판단도
`RoutineApplicationService`가 직접 소유하게 했다. 이번 추가 회차에는 URL context answer와 grounded web composition을
`IRoutineSearchGateway` / `CommandService.RoutineSearchGateway.cs`로 빼서 `RoutineLlmGatewayAdapter`는 provider generation만 남겼다.

**coding 이관 결과**:
기존 `CommandService.Coding*.cs` 10개 partial(`Coding.cs`, `CodingExecution.cs`, `CodingProfiles.cs`,
`CodingVerification.cs`, `CodingResultActions.cs`, `CodingWorkspace.cs`, `CodingPresentation.cs`, `CodingQuality.cs`,
`CodingDeterministicRepairs.cs`, `CodingProjectProfiles.cs`)과 `CommandService.Utils.cs`에 있던 coding loop 본문은
`Application/CodingApplicationService*.cs`로 이동했다. `CommandService.CodingGateway.cs` 370줄은 `CodingCommandGatewayAdapter`로
직접 인터페이스 구현을 끊고, LLM/입력 준비/Think+/citation/대화 제목/압축 등 아직 공통 상태가 필요한 부분만
명시적으로 bridge한다.

> 주의: 현재 `CommandService.Telegram.Coding.cs`는 텔레그램 전용 설정/파일 다운로드/최근 결과 표시 경로로 남아 있다.
> 이는 텔레그램 UX 보존용 direct 경로이며, coding 실행 본문은 `CodingAppService.RunCoding*Async`로 위임한다.

---

## 4. 핵심 문제 — 왜 "M4+M5 한방"이 불가능한가

1. **가짜 탈결합 핵심은 제거됐다.** routine과 coding 모두 `_inner: CommandService` 위임 래퍼가 아니며,
   오케스트레이션 본문과 slash handler는 application service/handler가 소유한다.

2. **남은 결합은 adapter 내부의 심층 상태 호출이다.** routine/coding app service는 더 이상 `CommandService`가
   직접 구현한 gateway를 받지 않고 `RoutineLlmGatewayAdapter`/`CodingCommandGatewayAdapter`를 받는다.
   그러나 adapter 내부가 아직 공통 LLM generation/search composition/input/citation/대화 helper 일부를 `CommandService` owner에서 호출한다.
   routine search composition은 `RoutineSearchGatewayAdapter`로 LLM gateway에서 분리했지만 실제 구현은 아직 CommandService에 남아 있고,
   routine logic graph 실행, search/url 모델 해석, Groq rate-limit 판단은 분리했지만, graph runtime core 자체는 아직
   `CommandService.LogicGraphs.cs`에 남아 있다.
   이 bridge를 완전히 정리하려면 공통 LLM/input/citation/search/logic graph 상태를 별도 service로 옮기는 후속 대형 작업이 필요하다.

3. **"가짜 이관" 금지.** 새 핸들러를 만들어 그냥 `_inner.Run…()` 위임 래퍼에 연결하면 컴파일·동작은 하지만
   God Object state를 계속 만지므로 **결합도가 전혀 줄지 않는다.** 플랜이 명시적으로 금지한 패턴이다.
   따라서 "핸들러만 빨리 추가" 하는 우회로가 없다.

4. **M5 안전 정리는 완료됐지만 완전 해결 승격은 보류.** 남은 것은 죽은 레거시가 아니라
   gateway adapter와 logic graph runner adapter 내부의 `CommandService` 상태 호출이다.

---

## 5. 해결 방법 — 단계별 로드맵

> 원칙: 각 단계는 **그 자체로 컴파일·테스트 GREEN**, 회귀를 계약/단위 테스트로 잠그고, 텔레그램 동작 보존,
> AOT 제약(리플렉션 금지·수동 `new` 등록) 준수.

### M4-R: routine 이관 (1~2 세션)

상태: **완료됨.** `RoutineApplicationService` 를 `_inner` 위임 래퍼에서 **진짜 구현**으로 전환했고,
`RoutineSlashCommandHandler`를 추가·와이어인했다.

1. **의존성 식별.** 기존 `CommandService.Routine*.cs` 가 만지던 공유 필드 집합을 추출했고,
   `_routineSchedulerTask`, `_llmRouter`, `_telegramClient`, `_paths` 등 루틴 소유 상태를 `RoutineApplicationService` 생성자 주입으로 옮겼다.
2. **오케스트레이션 이동.** `RoutineExecution`/`Routines`/`RoutineRunHelpers`/`RoutineGeneration*` 의 본문을
   `RoutineApplicationService` 로 옮긴다. 위 의존성을 **생성자 주입**으로 받는다. 순수 부분은 이미 있는
   `RoutineCommandPolicy` 등 static 정책으로 더 밀어낸다.
3. **CommandService 측 정리.** 옮긴 메서드는 호출부를 직접 app service/handler로 바꿨고,
   `RoutineApplicationService` 의 `_inner` 의존과 `CommandService.*` 정적 helper bridge를 제거했다.
4. **핸들러 추가.** `apps/omnux-middleware/src/CommandDispatch/RoutineSlashCommandHandler.cs` 신규.
   의존 = `IRoutineApplicationService`(이제 진짜 구현). `CanHandle` 은 `UnifiedSlashCommandPolicy.Parse` 로
   routine 토큰 판정. `HandleAsync` 는 기존 텍스트 routine 명령 동작을 재현.
5. **와이어인.** `Program.cs` 의 수동 handler list에 `new RoutineSlashCommandHandler(routineApplicationService)`를 추가했다.
   텔레그램 routine pseudo 경로도 `TryHandleViaSlashRouterAsync` bridge로 같은 라우터를 탄다.
6. **테스트.** `RoutineSlashCommandHandlerTests.cs` — fake `IRoutineApplicationService` 로 핸들러 단독 검증.
   기존 routine 동작(생성/실행/삭제/스케줄)이 보존되는지 회귀 테스트.

### M4-C: coding 이관 (2~3 세션, 가장 무거움)

routine과 동일 절차이되, coding은 **서브프로세스 검증 파이프라인**(`CodingVerification` 1370줄: 컴파일/실행/
stdout·stderr 캡처/타임아웃) 때문에 더 위험하다.

상태: **완료됨.** 오케스트레이션 이관, `_inner` 제거, slash handler/test/와이어인까지 끝났다.

1. coding 공유 필드 식별(`_codeRunner`, `_runArtifactStore`, 검증/샌드박스 관련) 완료.
2. `RunCodingSingle/Orchestration/Multi`, `ExecuteLatestCodingResult`, 검증·프로필·산출물 액션을
   `CodingApplicationService` 로 이동했고, 생성자 주입화했다. `_inner` 제거 완료.
3. `CommandService.CodingGateway.cs`를 추가해 공통 LLM/입력 준비/Think+/citation/대화 제목/압축 helper만 명시 bridge로 격리했다.
4. `CommandService.Coding*.cs` 10개는 삭제됐고, `Application/CodingApplicationService*.cs` 12개가 실제 구현을 소유한다.
5. `CodingSlashCommandHandler` 추가 + 와이어인 + `CodingSlashCommandHandlerTests` 완료.
6. coding은 보안 경계(샌드박스·의존성 격리)와 맞물리므로 `scripts/check-security-boundaries.mjs`,
   `scripts/check-coding-python-game-contract.mjs`, `scripts/check-browser-intent-contract.mjs` 회귀를 특히 주의.

### M5: 레거시 제거 + 결합도 재측정

상태: **안전하게 제거 가능한 레거시는 완료됨.** 완전 해결 승격은 아직 하지 않는다.

1. **죽은 레거시 경로 제거 완료.** `CommandService.RoutineCommands.cs` 삭제, post-routing의 `/routine` 재consult 제거,
   `/metrics`·`/kill`은 `CoreRuntimeSlashCommandHandler`로 이동했다.
2. **텔레그램 wrapper 제거 완료.** doctor/plan/task/notebook/memory 전용 wrapper partial 5개를 삭제하고, 공통 slash router와
   pseudo-command `slashRouter` delegate로 통일했다. 텔레그램 `/coding` direct 경로는 모바일 파일 UX 때문에 보존한다.
3. **kill guard 분리 완료.** UID/allowlist 확인 helper를 `KillTargetGuardPolicy`로 옮겨 `CommandService.Telegram.cs`에서 제거했다.
4. **routine 정적 bridge 제거 완료.** `RoutineApplicationService.CommandBridge.cs`가 `CommandService.*` 정적 helper를 호출하지 않는다.
5. **gateway 직접 구현 제거 완료.** `CommandService : ICodingCommandGateway/IRoutineLlmGateway` 상속을 제거하고,
   `CreateCodingCommandGateway()` / `CreateRoutineLlmGateway()`가 명시 adapter를 만들어 app service에 넘기도록 했다.
6. **routine 단순 상태 분리 완료.** dynamic-code flag, workspace root, URL 해석, grounded web 실패 판정, 텔레그램 응답 포맷팅은 routine gateway에서 제거하고
   `RoutineApplicationService`가 직접 처리한다.
7. **routine logic graph/model/rate-limit 1차 분리 완료.** `IRoutineLlmGateway`에서 `ExecuteLogicGraphRunCoreAsync`,
   search/url 모델 해석, Groq rate-limit 판단을 제거했다. logic graph 실행은 `IRoutineLogicGraphRunner` /
   `CommandService.RoutineLogicGraphRunner.cs`로 별도 bridge를 만들고, 모델/rate-limit 판단은 `RoutineApplicationService`가 직접 소유한다.
8. **routine search gateway 1차 분리 완료.** URL context answer와 grounded web composition을 `IRoutineLlmGateway`에서 제거하고
   `IRoutineSearchGateway` / `CommandService.RoutineSearchGateway.cs`로 별도 bridge를 만들었다.
9. **결합도 재측정 완료.** `CommandService*.cs`는 52파일, 24,378줄, private 필드 82개다.
10. **완전 해결 승격 보류.** gateway adapter 내부가 아직 `CommandService` owner를 통해 LLM/입력 준비/
   citation/search composition 공통 상태를 호출한다. routine search gateway와 logic graph runner도 아직 실제 service가 아니라 adapter 뒤에 남아 있다.
   이 상태 이전은 별도 대형 작업으로 분리한다.

---

## 6. 검증 파이프라인 (모든 단계 공통, GREEN 유지)

```bash
dotnet build apps/omnux-middleware/Omnux.Middleware.csproj
dotnet test  apps/omnux-middleware-tests/Omnux.Middleware.Tests.csproj   # 현재 1123개
node scripts/check-security-boundaries.mjs
node scripts/check-core-daemon-boundary-contract.mjs
node scripts/check-desktop-shell-boundary-contract.mjs
node scripts/check-tech-stack-contract.mjs
node scripts/check-coding-python-game-contract.mjs
node scripts/check-chat-telegram-contract.mjs
node scripts/check-gateway-runtime-contract.mjs
npm test
git diff --check
```

> 주의: `check-security-boundaries.mjs` / `check-tech-stack-contract.mjs` / `check-core-daemon-boundary-contract.mjs`
> 는 **`develop.md` 본문 문자열을 hard-assert** 한다. develop.md를 손대면 이 검사들이 깨질 수 있으니
> 상태 문구 변경 시 반드시 위 파이프라인을 다시 돌린다. (예: `"완료: 4건 (6번, 8번, 9번, 12번)"`,
> `"미완료률(완전 해결 기준): 67% (8/12)"`, `"SQLite/DB 큐 전환 최종 판단 완료"` 등.)

---

## 7. 한 세션에 안전하게 할 수 있는 것

### 7.1 옵션 비교

| 옵션 | 범위 | 위험 | 한 세션 | 가치 |
|---|---|---|---|---|
| **A. M5-lite** | 이미 이관된 도메인(doctor/notebook/plan/task/memory/llm/채널)의 죽은 레거시 웹/텔레그램 slash 경로 + 미사용 필드 제거, 결합도 재측정 | 낮음~중 | ✅ | 완료 |
| **B. routine 이관 (M4-R)** | 루틴 오케스트레이션 추출 + 핸들러 + 테스트 | 중 | △ (빠듯, 1~2세션) | 완료 |
| **C. coding 이관 (M4-C)** | coding 오케스트레이션 추출 + handler + 검증 파이프라인 | 높음 | ❌ (다중 세션) | 완료 |
| **D. M5 안전 레거시 제거** | routine slash bridge, core runtime slash, 텔레그램 wrapper, kill guard, routine 정적 bridge 정리 | 중 | ✅ | 완료 |
| **E. gateway adapter 경계** | `CommandService`의 gateway 인터페이스 직접 구현 제거, 명시 adapter 생성과 계약 고정 | 중 | ✅ | 완료 |
| **F. routine logic graph/model/rate-limit 분리** | `IRoutineLlmGateway`에서 logic graph 실행, search/url 모델 해석, Groq rate-limit 판단을 빼고 별도 소유권/계약 고정 | 중 | ✅ | 완료 |
| **G. routine search gateway 분리** | `IRoutineLlmGateway`에서 URL context answer와 grounded web composition을 빼고 별도 search gateway 계약 고정 | 중 | ✅ | 완료 |

### 7.2 다음 추천: gateway bridge 심층 분리

M5-lite, routine 이관, coding 이관, 안전 레거시 제거, gateway adapter 경계, routine logic graph/model/rate-limit 1차 분리,
routine search gateway 1차 분리는 끝났다.
다음 작업은 `CommandService.RoutineGateway`, `CommandService.RoutineSearchGateway`, `CommandService.CodingGateway`의 adapter 내부가 호출하는 LLM generation/search composition/input/citation
bridge를 실제 application/infrastructure service로 분리할 수 있는지 감사하는 것이다.
텔레그램 `/coding` direct 경로는 모바일 파일 다운로드와 최근 결과 표시 UX가 있으므로 보존한다.

### 7.3 ⚠️ 심층 bridge 분리 전 필수 확인

- **텔레그램 `/coding` direct 보존.** 모바일 파일 다운로드/status/last/files/file UX 때문에 일반 slash handler로
  단순 치환하지 않는다.
- **gateway method별 소유권 분리.** `ICodingCommandGateway`는 LLM provider 선택, input preparation, citation guard,
  conversation title/compress, Think+를 섞고 있다. method 단위로 새 service 후보를 쪼갠 뒤 이동한다.
- **routine LLM/search 경계.** `IRoutineLlmGateway`는 이제 LLM provider 호출만 갖는다. URL context answer와 grounded web composition은
  `IRoutineSearchGateway`로 빠졌고, logic graph 실행은 `IRoutineLogicGraphRunner`로 빠졌다. search/url 모델 해석과 Groq rate-limit 판단은
  routine app service가 직접 소유한다. search composition 구현체와 graph runtime core의 실제 service 분리는 아직 남아 있다.
- **develop.md 상태 문구.** 6장의 hard-assert 문자열을 깨지 않도록 변경 후 전체 파이프라인 재실행.

---

## 8. 위험 / 주의사항 요약

1. **AOT 제약.** `PublishAot=true`. 리플렉션 기반 DI/MediatR 금지. 핸들러는 `SlashCommandRouter` 조립 지점에
   **수동 `new`** 로만 등록.
2. **텔레그램 회귀.** 텔레그램은 일부 도메인 전용 동작(요약/handoff)이 있다. 이관·제거 시 텔레그램 direct
   경로 보존을 항상 먼저 확인.
3. **가짜 이관 금지.** 핸들러를 `_inner` 위임 래퍼에 연결하면 결합도가 안 줄어든다 — 반드시 구현을
   CommandService 밖으로 빼서 application service가 소유하게 한다.
4. **보안 경계.** coding 이관은 샌드박스/의존성 격리(결함 #3)와 맞물린다. 관련 계약 검사 회귀 주의.
5. **develop.md hard-assert.** 상태 문구가 계약 검사에 박혀 있다. 6장 주의 참고.
6. **브랜치 정책.** 이 저장소는 `main` 브랜치에서 직접 작업·커밋한다(사용자 지침). 별도 브랜치 생성 금지(명시 요청 시 예외).
   커밋 메시지에 Claude/Anthropic 귀속 trailer 금지.

---

## 9. 참고 파일 인덱스

| 목적 | 경로 |
|---|---|
| 핸들러 등록·라우터 조립 | `apps/omnux-middleware/src/Program.cs`, `apps/omnux-middleware/src/CommandService.SlashDispatch.cs` |
| 웹 strangler-fig 와이어인 | `apps/omnux-middleware/src/CommandService.Execution.Dispatch.cs` (≈L96) |
| 라우터/계약 seam | `apps/omnux-middleware/src/CommandDispatch/{SlashCommandRouter,ISlashCommandHandler,SlashCommandContext,SlashCommandTextFormat}.cs` |
| 이관 완료 핸들러 | `apps/omnux-middleware/src/CommandDispatch/*SlashCommandHandler.cs` (12종) |
| 핸들러 격리 테스트 | `apps/omnux-middleware-tests/*SlashCommandHandlerTests.cs` |
| coding slash handler | `apps/omnux-middleware/src/CommandDispatch/CodingSlashCommandHandler.cs` |
| 진짜 구현 app service(참고 모범) | `apps/omnux-middleware/src/Application/{Doctor,Notebook,Plan,TaskGraph,Memory,Conversation,LlmControl,LlmSettings,Routine,Coding}ApplicationService.cs` |
| routine 오케스트레이션(이관 완료) | `apps/omnux-middleware/src/Application/RoutineApplicationService*.cs`, `apps/omnux-middleware/src/CommandDispatch/RoutineSlashCommandHandler.cs` |
| coding 오케스트레이션(이관 완료) | `apps/omnux-middleware/src/Application/CodingApplicationService*.cs` |
| coding bridge | `apps/omnux-middleware/src/CommandService.CodingGateway.cs` |
| routine bridge | `apps/omnux-middleware/src/CommandService.RoutineGateway.cs` |
| core runtime slash handler | `apps/omnux-middleware/src/CommandDispatch/CoreRuntimeSlashCommandHandler.cs` |
| app service 주입 지점 | `apps/omnux-middleware/src/Program.cs` |
| 텔레그램 라우터 위임 | `apps/omnux-middleware/src/CommandService.Execution.Dispatch.cs`, `apps/omnux-middleware/src/CommandService.Telegram.LlmControl.cs` |
| 슬래시 토큰 파서(재사용) | `apps/omnux-middleware/src/UnifiedSlashCommandPolicy.cs` |
| 상위 설계 로드맵(M1~M5 원본) | `~/.claude/plans/expressive-finding-fox.md` |
| 상위 현황/검증 파이프라인 | `develop.md` |

---

## 10. 작업 체크리스트 (이어받는 에이전트용)

```
[x] 0. 이 문서 + ~/.claude/plans/expressive-finding-fox.md + develop.md 결함 #4 항목 읽기
[x] 1. 검증 파이프라인(6장)이 현재 GREEN인지 확인 (작업 시작 baseline)
[x] 옵션 A (M5-lite):
    [x] 이미 이관된 도메인의 레거시 텍스트 경로/중복 포맷터 식별
    [x] 텔레그램 direct 의존 없음 확인 (7.3)
    [x] CanHandle 전체 소유 확인 후 레거시 제거
    [x] 미사용 private 필드 제거
    [x] 결합도 3지표 재측정 → develop.md 기록 (hard-assert 주의)
    [x] 파이프라인 GREEN
[x] 옵션 B (M4-R routine):
    [x] routine 공유 필드 식별 → RoutineApplicationService로 오케스트레이션 이동(생성자 주입, _inner 제거)
    [x] RoutineSlashCommandHandler + 테스트 + 와이어인(웹·텔레그램)
    [x] 파이프라인 GREEN
[x] 옵션 C (M4-C coding):
    [x] coding 공유 필드 식별 → CodingApplicationService 생성자 주입화
    [x] `CommandService.Coding*.cs` 10개 + coding loop 본문을 `Application/CodingApplicationService*.cs`로 이동
    [x] `_inner` 위임 래퍼 제거, `CommandService.CodingGateway.cs` 명시 bridge 추가
    [x] 텔레그램/logic graph 호출부를 `CodingAppService.RunCoding*Async`로 전환
    [x] 보안/coding/browser intent 계약 스크립트 경로 갱신
    [x] CodingSlashCommandHandler + 테스트 + 와이어인
    [x] M4-C 기준 전체 검증 파이프라인 GREEN
[x] M5 안전 레거시 제거:
    [x] `CommandService.RoutineCommands.cs` 삭제, post-routing `/routine` 재consult 제거
    [x] `/metrics`·`/kill`을 `CoreRuntimeSlashCommandHandler`로 이동 + 테스트 추가
    [x] 텔레그램 doctor/plan/task/notebook/memory wrapper partial 5개 삭제
    [x] `KillTargetGuardPolicy` 분리
    [x] routine app service의 `CommandService.*` 정적 bridge 제거
    [x] `CommandService : ICodingCommandGateway/IRoutineLlmGateway` 직접 구현 제거 → 명시 adapter 생성
    [x] routine dynamic-code/workspace root/URL/grounded web failure/telegram formatting을 app service 직접 소유로 이동
    [x] routine logic graph 실행을 `IRoutineLlmGateway`에서 제거 → `IRoutineLogicGraphRunner` 별도 bridge 생성
    [x] routine search/url 모델 해석과 Groq rate-limit 판단을 app service 직접 소유로 이동
    [x] routine URL context answer와 grounded web composition을 `IRoutineLlmGateway`에서 제거 → `IRoutineSearchGateway` 별도 bridge 생성
    [x] 결합도 3지표 재측정 → 52파일, 24,378줄, private 필드 82개
[ ] 심층 상태 분리: gateway adapter 내부의 LLM generation/search composition/input/citation 호출과 routine search/logic graph runner core를 독립 service로 전환
```
