# 결함 #4 (CommandService God Object) — M4·M5 완전 해결 가이드

> 이 문서는 결함 #4 탈결합 작업의 **M4(무거운 도메인 이관)** 와 **M5(레거시 제거·결합도 재측정)** 를
> 다른 AI 에이전트나 개발자가 맥락 없이도 이어서 작업할 수 있도록, **문제 진단부터 단계별 해결 방법까지** 빠짐없이 정리한다.
>
> - 최종 작성: 2026-06-02
> - 대상 코드: `apps/omnux-middleware` (.NET 9, `PublishAot=true`)
> - 상위 설계: `~/.claude/plans/expressive-finding-fox.md` (M1~M5 로드맵 원본)
> - 상위 현황: [`develop.md`](develop.md) 의 결함 #4 항목 (현재 "1차 보강, M4·M5 진행 중")
>
> **한 줄 결론: M4와 M5를 한 세션에 한방으로 끝내는 것은 비현실적이다.** `/llm`·채널을 포함한
> 가벼운 도메인은 이미 이관 완료됐고, M4에 남은 것은 **routine + coding(약 13,034줄)의 오케스트레이션을
> God Object에서 진짜 application service로 추출**하는 작업이다. 이것이 결함 #4의 핵심(진짜 God Object)이며
> 다중 세션 작업이다. M5는 M4가 끝나야 닫을 수 있다.

---

## 0. TL;DR (의사결정용)

| 질문 | 답 |
|---|---|
| M4+M5 한방 가능? | ❌ 아니오. routine/coding 오케스트레이션 추출이 다중 세션 작업이고, M5는 M4에 종속. |
| M4에서 이미 끝난 것 | `/llm`(LlmControl) + 채널 설정(`/talk /code /profile /mode /provider /model /status`) 이관 완료. |
| M4에 남은 것 | **routine 이관 + coding 이관** (각각 별도 트랙). 진짜 God Object 핵심. |
| 왜 무거운가 | routine 4,629줄 + coding 8,405줄이 CommandService 안에 있고, app service는 16·23줄짜리 **위임 래퍼**(가짜 탈결합)일 뿐. |
| M5가 막힌 이유 | 레거시 텍스트 경로·미사용 필드 제거는 routine/coding이 핸들러로 빠진 뒤에만 가능. |
| 한 세션에 안전하게 할 수 있는 것 | **옵션 A: M5-lite** (이미 이관된 도메인의 죽은 레거시 경로·미사용 필드 제거 + 결합도 재측정). 7장 참고. |
| 추천 순서 | A(M5-lite) → B(routine 이관) → C(coding 이관) → M5(최종 레거시 제거·재측정). |

---

## 1. 배경 — 결함 #4란 무엇인가

`CommandService` 는 웹 대시보드와 텔레그램 봇의 모든 명령이 통과하는 미들웨어의 척추다. 시간이 지나며
하나의 `partial class` 가 거대해졌다.

**현재 측정치 (2026-06-02, `apps/omnux-middleware/src` 기준):**

| 지표 | 값 | 측정 명령 |
|---|---|---|
| `CommandService*.cs` partial 파일 수 | **79** | `ls CommandService*.cs \| wc -l` |
| 총 줄 수 | **38,743** | `cat CommandService*.cs \| wc -l` |
| 구분되는 private 필드 수 | **91** | `grep -hoE "private [A-Za-z0-9_<>,?. ]+ _[a-zA-Z0-9]+" CommandService*.cs \| grep -oE "_[a-zA-Z0-9]+$" \| sort -u \| wc -l` |

`develop.md` 의 `3.6 팩트폭행 리뷰` 가 스스로 진단한 핵심:

> "Partial 클래스와 Policy로 나눈 것은 코드를 **물리적으로 분산**시켰을 뿐, 아키텍처의 **논리적 결합도는
> 낮추지 못했다**. (= 가짜 리팩터링)"

즉 파일만 쪼갰을 뿐, 80개 partial이 동일한 91개 공유 가변 private 필드(`_conversationStore`, `_context`,
`_auditLogger`, `_llmRouter`, `_codeRunner`, `_runArtifactStore`, `_routineSchedulerTask` …)를 직접 만진다.
한 곳을 고치면 다른 곳이 깨지는 진짜 God Object다.

### "완전 해결" 의 정의

라우터가 명령을 도메인 핸들러에 위임하고, **각 핸들러가 CommandService private state가 아니라 자기 도메인
ApplicationService(진짜 구현)만 의존**하도록 dispatch 경계를 세워 91필드 결합을 **실제로** 줄이는 것.
파일을 더 쪼개는 것이 아니라, **상태 의존을 끊는 것**이 목표다.

---

## 2. 채택한 아키텍처 — strangler-fig + AOT-safe 수동 라우터

### 2.1 왜 이 패턴인가

- 이 코드베이스에는 이미 정답 패턴이 있다: **WebSocket 측 `Ws*CommandDispatcher` 12개**는
  `internal sealed`, 생성자로 ApplicationService 주입, `TryHandleAsync → bool`, CommandService 미의존 형태로
  이미 분리돼 있다. (단, 2.4의 함정 주의)
- `Omnux.Middleware.csproj` 는 **`PublishAot=true`** → 리플렉션 기반 MediatR/DI 컨테이너는 **비호환**.
  따라서 새 프레임워크를 도입하지 않고, 검증된 Ws dispatcher 패턴을 **텍스트/슬래시 명령 경로에 그대로** 적용한다.
- **strangler-fig(교살자 무화과):** 도메인을 하나씩 새 핸들러로 떼어내고, 라우터가 "소유 핸들러가 없으면
  `null` → 레거시 경로로 fall-through" 하게 해서 회귀 없이 점진 이관한다. 마지막에 레거시 경로와 그때
  쓰이지 않게 된 private 필드를 제거한다(= M5).

### 2.2 라우팅 골격

```
ExecuteNormalizedCommandRoutingAsync (라우터, 얇음)
  → (텔레그램 direct 보존 경로 먼저)
  → SlashCommandRouter.TryHandleAsync(ctx)        // 등록 핸들러를 순서대로 consult
      → StaticSlashCommandHandler
      → DoctorSlashCommandHandler   (의존: IDoctorApplicationService)
      → NotebookSlashCommandHandler (의존: INotebookApplicationService)
      → HandoffSlashCommandHandler / PlanSlashCommandHandler / TaskSlashCommandHandler
      → MemorySlashCommandHandler / ChannelSettingsSlashCommandHandler / LlmControlSlashCommandHandler
  → (miss = null) 레거시 CommandService unified-slash 경로로 fall-through   // strangler-fig
```

### 2.3 코드 상의 핵심 지점 (인용)

**핸들러 수동 등록 (AOT-safe, 리플렉션 없음)** — [`CommandService.SlashDispatch.cs:23`](apps/omnux-middleware/src/CommandService.SlashDispatch.cs)

```csharp
private SlashCommandRouter SlashCommandRouter =>
    _slashCommandRouter ??= new SlashCommandRouter(new ISlashCommandHandler[]
    {
        new StaticSlashCommandHandler(),
        new DoctorSlashCommandHandler(_doctorAppService),
        new NotebookSlashCommandHandler(_notebookAppService),
        new HandoffSlashCommandHandler(_notebookAppService),
        new PlanSlashCommandHandler(_planAppService),
        new TaskSlashCommandHandler(_taskGraphAppService),
        new MemorySlashCommandHandler(_memoryAppService, _conversationAppService),
        new ChannelSettingsSlashCommandHandler(_llmSettingsAppService),
        new LlmControlSlashCommandHandler(LlmControlService),
    });
```

**웹 경로 strangler-fig 와이어인** — [`CommandService.Execution.Dispatch.cs:96`](apps/omnux-middleware/src/CommandService.Execution.Dispatch.cs)

```csharp
// 텔레그램 direct(텔레그램 전용 동작 보존) 다음, 레거시 unified slash 앞에서 consult한다.
var slashRouterResult = await TryHandleViaSlashRouterAsync(text, source, cancellationToken);
if (slashRouterResult != null)
{
    return slashRouterResult;   // 소유 핸들러가 처리
}
return await ExecutePostUnifiedRoutingAsync(...);   // miss → 레거시 fall-through
```

**텔레그램 경로도 동일 라우터로 위임** — 예: [`CommandService.Telegram.Doctor.cs:15`](apps/omnux-middleware/src/CommandService.Telegram.Doctor.cs)

```csharp
private async Task<string?> TryHandleTelegramDoctorCommandAsync(string text, CancellationToken ct)
{
    if (!text.StartsWith("/doctor", StringComparison.OrdinalIgnoreCase)) return null;
    return await TryHandleViaSlashRouterAsync(text, "telegram", ct);
}
```

> 텔레그램 consult 지점: `Telegram.Doctor.cs`, `Telegram.Notebooks.cs`, `Telegram.Plans.cs`,
> `Telegram.Tasks.cs`, `Telegram.MemoryCommand.cs`. 즉 이관된 도메인은 **웹과 텔레그램 양쪽** 모두 라우터를 탄다.

### 2.4 ⚠️ 핵심 함정 — "인터페이스 탈결합 ≠ 진짜 탈결합"

`Ws*CommandDispatcher` 가 `IRoutineApplicationService` 를 의존한다고 해서 routine이 탈결합된 것이 **아니다.**
그 인터페이스의 **구현체가 CommandService로 되돌아가는 위임 래퍼**이면, 오케스트레이션은 여전히 God Object 안에 있다.

[`Application/RoutineApplicationService.cs`](apps/omnux-middleware/src/Application/RoutineApplicationService.cs) (전체 23줄):

```csharp
public sealed class RoutineApplicationService : IRoutineApplicationService
{
    private readonly CommandService _inner;                       // ← God Object로 되돌림
    public RoutineApplicationService(CommandService inner) { _inner = inner; }

    public IReadOnlyList<RoutineSummary> ListRoutines() => _inner.ListRoutines();
    public Task<RoutineActionResult> CreateRoutineAsync(...) => _inner.CreateRoutineAsync(...);
    // ... 모든 메서드가 _inner.* 로 전달만 한다
}
```

[`Application/CodingApplicationService.cs`](apps/omnux-middleware/src/Application/CodingApplicationService.cs) (전체 16줄) 도 동일하게
`_inner.RunCodingSingleAsync(...)` 처럼 전부 전달만 한다. 주입 위치는
[`Program.cs:212`](apps/omnux-middleware/src/Program.cs) `new RoutineApplicationService(commandService)`,
[`Program.cs:756`](apps/omnux-middleware/src/Program.cs) `new CodingApplicationService(commandService)`.

**이 함정이 M4의 본질이다.** routine/coding을 진짜 탈결합하려면 위임 방향을 **뒤집어야** 한다:
구현(오케스트레이션)을 CommandService 밖으로 빼서 application service가 **소유**하게 하고, CommandService가
(필요하면) 그 service에 위임하거나 아예 그 코드를 들어내야 한다.

---

## 3. 현재 진행 상태 — 무엇이 끝났고 무엇이 남았나

### 3.1 이관 완료 핸들러 (9종, 와이어됨)

| 핸들러 | 명령 | 의존 application service | service 상태 |
|---|---|---|---|
| `StaticSlashCommandHandler` | 정적 help/usage 텍스트 | (순수, 없음) | — |
| `DoctorSlashCommandHandler` | `/doctor` | `IDoctorApplicationService` | ✅ 진짜 구현 134줄 |
| `NotebookSlashCommandHandler` | `/notebook` | `INotebookApplicationService` | ✅ 진짜 구현 51줄 |
| `HandoffSlashCommandHandler` | `/handoff` | `INotebookApplicationService` | ✅ 진짜 구현 |
| `PlanSlashCommandHandler` | `/plan` | `IPlanApplicationService` | ✅ 진짜 구현 443줄 |
| `TaskSlashCommandHandler` | `/task` | `ITaskGraphApplicationService` | ✅ 진짜 구현 114줄 |
| `MemorySlashCommandHandler` | `/memory` | `IMemoryApplicationService` + `IConversationApplicationService` | ✅ 진짜 구현 193 / 1420줄 |
| `ChannelSettingsSlashCommandHandler` | `/talk /code /profile /mode /provider /model /status model` | `ILlmSettingsApplicationService` | ✅ 진짜 구현 542줄 |
| `LlmControlSlashCommandHandler` | `/llm` (help/usage/models/set …) | `ILlmControlApplicationService` | ✅ 진짜 구현 331줄 |

핸들러 격리 테스트: `apps/omnux-middleware-tests/*SlashCommandHandlerTests.cs` — 각 핸들러를 **CommandService 없이
fake application service만으로** 단독 생성·검증한다. 이 테스트의 존재 자체가 "God Object 없이 동작한다"는 탈결합 증명이다.

### 3.2 이관 안 된 도메인 (M4 잔여)

| 도메인 | app service 상태 | 실제 오케스트레이션 위치 | 줄 수 |
|---|---|---|---|
| **routine** | ❌ 위임 래퍼 23줄 (`_inner` 포워딩) | `CommandService.Routine*.cs` (12개 partial, `Routines.cs` 포함) | **4,629** |
| **coding** | ❌ 위임 래퍼 16줄 (`_inner` 포워딩) | `CommandService.Coding*.cs` (10개 partial) | **8,405** |
| | | **합계** | **13,034** |

이관된 도메인의 app service는 전부 `_inner` 없는 진짜 구현(로직이 실제로 CommandService 밖에 있음)인데,
routine/coding만 위임 래퍼다. 이 대비가 "남은 일이 곧 결함 #4의 핵심"임을 보여준다.

**routine partial 목록** (`CommandService.Routine*.cs`, `CommandService.Routines.cs`):
`Routines.cs`(1141), `RoutineExecution.cs`(1927), `RoutineCommands.cs`(309), `RoutineGeneration.cs`(44),
`RoutineGeneration.Execution.cs`(149), `RoutineGenerationStrategy.cs`(60), `RoutineGenerationText.cs`(167),
`RoutinePrompts.cs`(55), `RoutineRunHelpers.cs`(308), `RoutineScheduler.cs`(64), `RoutineSummary.cs`(235),
`RoutineValidation.cs`(170).

**coding partial 목록** (`CommandService.Coding*.cs`):
`Coding.cs`(1704), `CodingExecution.cs`(1292), `CodingProfiles.cs`(1592), `CodingVerification.cs`(1370),
`CodingResultActions.cs`(826), `CodingWorkspace.cs`(510), `CodingPresentation.cs`(382), `CodingQuality.cs`(397),
`CodingDeterministicRepairs.cs`(212), `CodingProjectProfiles.cs`(120).

> 주의: 위 13,034줄 전부가 그대로 application service로 옮겨가는 것은 아니다. 상당 부분은 이미 순수
> static 정책(`RoutineCommandPolicy` 등)으로 분리돼 있거나 옮길 수 있다. 그러나 **실행 진입부**
> (`RoutineExecution`, `CodingExecution`, `CodingVerification`)는 스케줄러 태스크, LLM 라우터, 코드 러너,
> 산출물 저장소, 서브프로세스 검증 파이프라인 등 **stateful 공유 필드**에 깊게 묶여 있어, 이 의존을 끊는 것이 핵심 난이도다.

---

## 4. 핵심 문제 — 왜 "M4+M5 한방"이 불가능한가

1. **routine/coding은 가짜 탈결합 상태다 (2.4).** 인터페이스는 있지만 구현이 God Object로 되돌아간다.
   진짜 이관 = 13,034줄 규모의 오케스트레이션을 CommandService 밖으로 빼는 일. 한 세션 분량이 아니다.

2. **깊은 상태 결합.** routine 실행은 `_routineSchedulerTask`/`_llmRouter`/`_telegramClient` 등에,
   coding 검증은 코드 러너/서브프로세스/산출물 저장소 등에 직접 묶여 있다. 이들을 application service가
   소유하려면 의존성을 생성자 주입으로 끌어올리고, 여러 partial에 흩어진 호출부를 전부 바꿔야 한다.

3. **"가짜 이관" 금지.** 새 핸들러를 만들어 그냥 `_inner.Run…()` 위임 래퍼에 연결하면 컴파일·동작은 하지만
   God Object state를 계속 만지므로 **결합도가 전혀 줄지 않는다.** 플랜이 명시적으로 금지한 패턴이다.
   따라서 "핸들러만 빨리 추가" 하는 우회로가 없다.

4. **M5는 M4에 종속.** 레거시 텍스트 라우팅 경로와 그때 미사용이 된 private 필드 제거는 routine/coding이
   핸들러로 빠진 **뒤에야** 안전하다. routine/coding이 남아 있는 한 그 도메인이 쓰는 필드는 못 지운다.
   → M4 미완 = M5 미완.

---

## 5. 해결 방법 — 단계별 로드맵

> 원칙: 각 단계는 **그 자체로 컴파일·테스트 GREEN**, 회귀를 계약/단위 테스트로 잠그고, 텔레그램 동작 보존,
> AOT 제약(리플렉션 금지·수동 `new` 등록) 준수.

### M4-R: routine 이관 (1~2 세션)

목표: `RoutineApplicationService` 를 `_inner` 위임 래퍼에서 **진짜 구현**으로 전환하고, `RoutineSlashCommandHandler`
를 추가·와이어인.

1. **의존성 식별.** `CommandService.Routine*.cs` 가 만지는 공유 필드 집합을 추출한다
   (예: `_routineSchedulerTask`, `_routineStore`, `_llmRouter`, `_telegramClient`, `_paths`, `_auditLogger` …).
   `grep -oE '_[a-zA-Z][a-zA-Z0-9]*' CommandService.Routine*.cs | sort | uniq -c | sort -rn` 로 시작하되,
   로컬 변수와 진짜 필드를 구분할 것(필드 정의와 대조).
2. **오케스트레이션 이동.** `RoutineExecution`/`Routines`/`RoutineRunHelpers`/`RoutineGeneration*` 의 본문을
   `RoutineApplicationService` 로 옮긴다. 위 의존성을 **생성자 주입**으로 받는다. 순수 부분은 이미 있는
   `RoutineCommandPolicy` 등 static 정책으로 더 밀어낸다.
3. **CommandService 측 정리.** 옮긴 메서드는 (a) 한동안 app service에 위임하거나 (b) 호출부를 직접 app service로
   바꾼다. `RoutineApplicationService` 의 `_inner` 의존을 제거(역방향 위임 종료).
4. **핸들러 추가.** `apps/omnux-middleware/src/CommandDispatch/RoutineSlashCommandHandler.cs` 신규.
   의존 = `IRoutineApplicationService`(이제 진짜 구현). `CanHandle` 은 `UnifiedSlashCommandPolicy.Parse` 로
   routine 토큰 판정. `HandleAsync` 는 기존 텍스트 routine 명령 동작을 재현.
5. **와이어인.** `CommandService.SlashDispatch.cs` 의 핸들러 배열에 `new RoutineSlashCommandHandler(_routineAppService)` 추가.
   텔레그램 routine 경로도 `TryHandleViaSlashRouterAsync` 위임으로 통일(기존 Telegram.Doctor 패턴 미러).
6. **테스트.** `RoutineSlashCommandHandlerTests.cs` — fake `IRoutineApplicationService` 로 핸들러 단독 검증.
   기존 routine 동작(생성/실행/삭제/스케줄)이 보존되는지 회귀 테스트.

### M4-C: coding 이관 (2~3 세션, 가장 무거움)

routine과 동일 절차이되, coding은 **서브프로세스 검증 파이프라인**(`CodingVerification` 1370줄: 컴파일/실행/
stdout·stderr 캡처/타임아웃) 때문에 더 위험하다.

1. coding 공유 필드 식별(`_codeRunner`, `_runArtifactStore`, 검증/샌드박스 관련).
2. `RunCodingSingle/Orchestration/Multi`, `ExecuteLatestCodingResult`, 검증·프로필·산출물 액션을
   `CodingApplicationService` 로 이동, 생성자 주입화. `_inner` 제거.
3. `CodingSlashCommandHandler` 추가 + 와이어인 + `CodingSlashCommandHandlerTests`.
4. coding은 보안 경계(샌드박스·의존성 격리)와 맞물리므로 `scripts/check-security-boundaries.mjs`,
   `scripts/check-coding-python-game-contract.mjs` 회귀를 특히 주의.

### M5: 레거시 제거 + 결합도 재측정 (M4 완료 후, 1 세션)

1. **죽은 레거시 경로 제거.** routine/coding까지 라우터가 소유하면, 레거시 unified-slash fall-through 경로와
   중복 텍스트 실행 메서드를 삭제. 텔레그램 direct 경로가 그 메서드에 의존하지 않는지 먼저 확인.
2. **미사용 private 필드 제거.** 이관 후 더 이상 참조되지 않는 필드를 컴파일러 경고/`grep` 으로 찾아 삭제.
3. **결합도 재측정.** 1장 표의 3개 지표(파일 수·줄 수·필드 수)를 다시 측정해 `develop.md` 에 기록.
   목표: private 필드 91 → 유의미하게 감소, partial 줄 수 대폭 감소.
4. **`develop.md` 상태 전환.** 결함 #4를 "1차 보강" → "완전 해결"로 승격(사용자 합의 하에). 계약 검사
   문자열(`scripts/check-tech-stack-contract.mjs` 의 "완료: N건 …" 등)도 함께 갱신. ⚠️ 7.3 주의.

---

## 6. 검증 파이프라인 (모든 단계 공통, GREEN 유지)

```bash
dotnet build apps/omnux-middleware/Omnux.Middleware.csproj
dotnet test  apps/omnux-middleware-tests/Omnux.Middleware.Tests.csproj   # 현재 1114개
node scripts/check-security-boundaries.mjs
node scripts/check-core-daemon-boundary-contract.mjs
node scripts/check-desktop-shell-boundary-contract.mjs
node scripts/check-tech-stack-contract.mjs
node scripts/check-coding-python-game-contract.mjs
node scripts/check-chat-telegram-contract.mjs
node scripts/check-gateway-runtime-contract.mjs
npm test
git diff --check
```

> 주의: `check-security-boundaries.mjs` / `check-tech-stack-contract.mjs` / `check-core-daemon-boundary-contract.mjs`
> 는 **`develop.md` 본문 문자열을 hard-assert** 한다. develop.md를 손대면 이 검사들이 깨질 수 있으니
> 상태 문구 변경 시 반드시 위 파이프라인을 다시 돌린다. (예: `"완료: 4건 (6번, 8번, 9번, 12번)"`,
> `"미완료률(완전 해결 기준): 67% (8/12)"`, `"SQLite/DB 큐 전환 최종 판단 완료"` 등.)

---

## 7. 한 세션에 안전하게 할 수 있는 것

### 7.1 옵션 비교

| 옵션 | 범위 | 위험 | 한 세션 | 가치 |
|---|---|---|---|---|
| **A. M5-lite** | 이미 이관된 도메인(doctor/notebook/plan/task/memory/llm/채널)의 죽은 레거시 웹/텔레그램 slash 경로 + 미사용 필드 제거, 결합도 재측정 | 낮음~중 | ✅ | 측정 가능한 God Object 축소 |
| **B. routine 이관 (M4-R)** | 4,629줄 오케스트레이션 추출 + 핸들러 + 테스트 | 중 | △ (빠듯, 1~2세션) | 결함 #4 본체 진입 |
| **C. coding 이관 (M4-C)** | 8,405줄 + 검증 파이프라인 | 높음 | ❌ (다중 세션) | — |

### 7.2 추천: A (M5-lite) 먼저

routine/coding 본작업(B·C)에 들어가기 전에, **이미 이관이 끝난 도메인의 잔재**를 청소해 실제 결합도를
한 단계 낮추고 그 수치를 확보한다. 핸들러와 격리 테스트가 이미 동작을 증명하므로 레거시 경로 제거는 비교적 안전하다.

### 7.3 ⚠️ M5-lite 진입 전 필수 확인

- **텔레그램 의존성.** 제거하려는 레거시 텍스트 메서드를 텔레그램 direct 경로가 아직 호출하지 않는지 확인.
  현재 텔레그램은 doctor/notebook/plan/task/memory에 대해 `TryHandleViaSlashRouterAsync` 로 위임하므로
  대체로 안전하지만, 각 도메인별로 grep 으로 잔여 호출을 직접 확인할 것.
- **fall-through 동작 보존.** 라우터 핸들러의 `CanHandle` 이 그 도메인 전 명령을 빠짐없이 소유하는지 확인한
  뒤에만 레거시 경로를 지운다(부분 소유면 미처리 명령이 사라진다).
- **develop.md 상태 문구.** 6장의 hard-assert 문자열을 깨지 않도록 변경 후 전체 파이프라인 재실행.

---

## 8. 위험 / 주의사항 요약

1. **AOT 제약.** `PublishAot=true`. 리플렉션 기반 DI/MediatR 금지. 핸들러는 `SlashCommandRouter` 배열에
   **수동 `new`** 로만 등록.
2. **텔레그램 회귀.** 텔레그램은 일부 도메인 전용 동작(요약/handoff)이 있다. 이관·제거 시 텔레그램 direct
   경로 보존을 항상 먼저 확인.
3. **가짜 이관 금지.** 핸들러를 `_inner` 위임 래퍼에 연결하면 결합도가 안 줄어든다 — 반드시 구현을
   CommandService 밖으로 빼서 application service가 소유하게 한다.
4. **보안 경계.** coding 이관은 샌드박스/의존성 격리(결함 #3)와 맞물린다. 관련 계약 검사 회귀 주의.
5. **develop.md hard-assert.** 상태 문구가 계약 검사에 박혀 있다. 6장 주의 참고.
6. **브랜치 정책.** 이 저장소는 `main` 브랜치에서 직접 작업·커밋한다(사용자 지침). 별도 브랜치 생성 금지(명시 요청 시 예외).
   커밋 메시지에 Claude/Anthropic 귀속 trailer 금지.

---

## 9. 참고 파일 인덱스

| 목적 | 경로 |
|---|---|
| 핸들러 등록·라우터 조립 | `apps/omnux-middleware/src/CommandService.SlashDispatch.cs` |
| 웹 strangler-fig 와이어인 | `apps/omnux-middleware/src/CommandService.Execution.Dispatch.cs` (≈L96) |
| 라우터/계약 seam | `apps/omnux-middleware/src/CommandDispatch/{SlashCommandRouter,ISlashCommandHandler,SlashCommandContext,SlashCommandTextFormat}.cs` |
| 이관 완료 핸들러 | `apps/omnux-middleware/src/CommandDispatch/*SlashCommandHandler.cs` (9종) |
| 핸들러 격리 테스트 | `apps/omnux-middleware-tests/*SlashCommandHandlerTests.cs` |
| 가짜 탈결합 래퍼(이관 대상) | `apps/omnux-middleware/src/Application/{Routine,Coding}ApplicationService.cs` |
| 진짜 구현 app service(참고 모범) | `apps/omnux-middleware/src/Application/{Doctor,Notebook,Plan,TaskGraph,Memory,Conversation,LlmControl,LlmSettings}ApplicationService.cs` |
| routine 오케스트레이션(이관 소스) | `apps/omnux-middleware/src/CommandService.Routine*.cs`, `CommandService.Routines.cs` |
| coding 오케스트레이션(이관 소스) | `apps/omnux-middleware/src/CommandService.Coding*.cs` |
| app service 주입 지점 | `apps/omnux-middleware/src/Program.cs` (L212 routine, L756 coding) |
| 텔레그램 라우터 위임 예시 | `apps/omnux-middleware/src/CommandService.Telegram.{Doctor,Notebooks,Plans,Tasks,MemoryCommand}.cs` |
| 슬래시 토큰 파서(재사용) | `apps/omnux-middleware/src/UnifiedSlashCommandPolicy.cs` |
| 상위 설계 로드맵(M1~M5 원본) | `~/.claude/plans/expressive-finding-fox.md` |
| 상위 현황/검증 파이프라인 | `develop.md` |

---

## 10. 작업 체크리스트 (이어받는 에이전트용)

```
[ ] 0. 이 문서 + ~/.claude/plans/expressive-finding-fox.md + develop.md 결함 #4 항목 읽기
[ ] 1. 검증 파이프라인(6장)이 현재 GREEN인지 확인 (작업 시작 baseline)
[ ] 옵션 A (M5-lite):
    [ ] 이미 이관된 도메인의 레거시 텍스트 경로/중복 포맷터 식별
    [ ] 텔레그램 direct 의존 없음 확인 (7.3)
    [ ] CanHandle 전체 소유 확인 후 레거시 제거
    [ ] 미사용 private 필드 제거
    [ ] 결합도 3지표 재측정 → develop.md 기록 (hard-assert 주의)
    [ ] 파이프라인 GREEN → 커밋
[ ] 옵션 B (M4-R routine):
    [ ] routine 공유 필드 식별 → RoutineApplicationService로 오케스트레이션 이동(생성자 주입, _inner 제거)
    [ ] RoutineSlashCommandHandler + 테스트 + 와이어인(웹·텔레그램)
    [ ] 파이프라인 GREEN → 커밋
[ ] 옵션 C (M4-C coding): B와 동일 절차, 검증 파이프라인 위험 가중
[ ] M5 최종: 레거시 전면 제거 + 필드 정리 + 재측정 + develop.md "완전 해결" 승격(사용자 합의)
```
