# 디렉터리 가이드

[한국어](./디렉터리_가이드.md) · [English](./en/directory-guide.md)

업데이트 기준: 2026-09-12

canonical 레이아웃은 `apps/`, `docs/`, `workspace/`다. 새 코드는 canonical 경로에만 둔다.

## 루트

| 경로 | 설명 |
|---|---|
| `apps/omnux-middleware/` | .NET 9 미들웨어. WebSocket/HTTP, 텔레그램, 라우팅, 도메인 오케스트레이션 |
| `apps/omnux-middleware-tests/` | .NET 단위 테스트 |
| `apps/desktop/` | Tauri v2 데스크톱 앱. React 19 + TypeScript + Tailwind CSS v4 |
| `apps/omnux-sandbox/` | Python 실행기 (`executor.py`) |
| `apps/shared/` | 모델 레지스트리(`model-registry.json`)와 C# 생성기 |
| `docs/` | 한국어 문서, `docs/en/` 영어 문서 |
| `scripts/` | `omnux` 실행기, Windows용 `omnux.ps1`, 계약/화면 검사 스크립트 |
| `deploy/` | macOS/Linux 배포 템플릿 |
| `.omni/skills/` | 프로젝트 스킬 (`SKILL.md`) |
| `workspace/` | 작업 산출물 (git 추적 안 함) |
| `output/` | 검사 스크린샷과 픽스처 (git 추적 안 함) |

## 미들웨어

| 경로 | 설명 |
|---|---|
| `apps/omnux-middleware/src/` | C# 원본 |
| `apps/omnux-middleware/src/Application/` | 도메인 서비스. Coding, Routine, Doctor, Plan, TaskGraph 등 |
| `apps/omnux-middleware/src/CommandDispatch/` | 슬래시 명령 라우터와 도메인 핸들러 |
| `apps/omnux-middleware/src/Infrastructure/` | Browser, Paths, Persistence, Refactor, Search, Telegram, Workspace |
| `apps/omnux-middleware/resources/browser/` | Playwright 브라우저 실행 리소스 (embedded) |

## 데스크톱

| 경로 | 설명 |
|---|---|
| `apps/desktop/src/App.tsx` | 화면 레지스트리 |
| `apps/desktop/src/features/` | 화면별 디렉터리 (`*-workspace`, `shell`, `settings`, `middleware` 등) |
| `apps/desktop/src/features/middleware/` | WebSocket gateway 헬퍼 |
| `apps/desktop/src/components/` | 공용 UI (`ui/primitives.tsx`, `screen/`, `capsule/`) |
| `apps/desktop/src-tauri/` | Tauri Rust 셸 (창 관리, 미들웨어 bootstrap) |

## 작업 산출물

| 경로 | 내용 |
|---|---|
| `workspace/coding/runs/` | 빌드 실행별 폴더 |
| `workspace/coding/routines/` | 루틴 실행 결과와 브라우저 에이전트 자산 |
| `workspace/.runtime/logic/` | 로직 그래프 실행 로그와 snapshot |
| `workspace/.runtime/tasks/` | task graph 실행 로그 |
| `workspace/.runtime/refactor-preview/` | Safe Refactor preview |

## 보존 기준

`~/.omnux`는 영속 상태 원본이다. 대화, 계획, 노트북, 라우팅 정책, 세션, 프로젝트, 에이전트 통신, telemetry, 메모리 노트가 들어 있으므로 삭제 전에는 반드시 보존 여부를 확인한다. `omnux` 실행기 상태는 `~/.omnux/cli/`에 있다.

## 실행기

- `./scripts/omnux setup`: 의존성 확인/설치, 빌드, `npm test`, 실행기 등록
- `omnux`: 데스크톱 앱 실행 (미들웨어 포함)
- `omnux start` / `status` / `shutdown`: 미들웨어만 실행, 상태 확인, 전체 종료
- `.\scripts\omnux.ps1 setup`: Windows setup
