# 사용법

[한국어](./사용법_빠른시작.md) · [English](./en/usage.md)

업데이트 기준: 2026-09-12

데스크톱 앱 화면별로 무엇을 누르는지 적었다. 설치와 실행은 [5분 시작](./QUICKSTART.md), 텔레그램은 [텔레그램 봇 가이드](./텔레그램_봇_가이드.md)를 본다.

## 화면 구성

왼쪽 레일에서 영역을 고르고 서브패널에서 화면을 고른다. 폭이 좁으면 상단 메뉴 버튼으로 내비를 연다. `⌘K`(Windows·Linux는 `Ctrl+K`)로 커맨드 팔레트를 연다.

| 영역 | 화면 |
|---|---|
| 홈 | 홈 |
| 워크스페이스 | 질문, 빌드, 자동화, 탐색, 리뷰 |
| 프로젝트 | 프로젝트, 작업, 노트 |
| 엔진 | 에이전트, 도구, 확장, 라우팅, 규칙 |
| 모니터 | 활동, 로그, 상태 |
| 설정 | 설정 |

## 홈

입력창에 바로 요청을 적는다. 아래 바로가기(자동화, 로직, 스킬, 계획, 노트)로 새 항목을 만들고, 하단 **이어서 작업하기**와 **활성 프로젝트**에서 최근 작업으로 돌아간다.

## 질문

탭: 대화 · 모델 · 참고 자료 · 기록

- 모드는 싱글, 오케스트레이션, 멀티다. 멀티는 여러 provider 답변을 나란히 놓고 비교한다.
- Enter로 보내고 Shift+Enter로 줄을 바꾼다. 한글 조합 중 Enter는 보내지 않는다.
- 파일과 이미지를 첨부한다. 이미지는 형식과 모델 지원을 먼저 확인한다.
- 답변은 노트, 작업, 빌드, 자동화로 넘길 수 있다.
- `네이버 열어줘`, `브라우저 닫아줘` 같은 요청은 LLM 호출 전에 브라우저 명령으로 처리한다.
- 스킬 이름을 말하면 그 스킬이 켜지고, 끌 때까지 같은 대화에서 유지된다.

## 빌드

탭: 빌드 · 설정 · 참고 · 기록

1. 만들 내용을 적고 **만들기**를 누른다. `Cmd/Ctrl+Enter`로도 보낸다. 파일은 최대 6개, 전체 10MB까지 첨부한다.
2. 진행 중에는 **작업 중단**으로 멈춘다. 다음 요청은 미리 적어 둘 수 있다.
3. 결과의 요약·파일·프로그램 출력을 읽는다. HTML은 **미리 보기**로 연다.
4. **다시 실행하기**로 입력을 넣어 결과를 다시 실행한다.
5. 멈춘 작업은 **중단한 요청 이어 쓰기**로 원래 요청과 모델을 되살린다.

모델·언어·프로젝트 이름은 **모델과 작업 설정**, 스킬·노트는 **스킬과 참고 노트**에서 고른다. 지난 빌드는 **저장한 빌드**에서 다시 연다. 실행 폴더는 `workspace/coding/runs/`다.

빌드에서도 `네이버 열어줘`, `브라우저 닫아줘`처럼 적으면 코딩 실행 전에 브라우저 명령으로 처리한다.

## 자동화

탭: 목록 · 새 자동화(편집) · 결과

1. **새 자동화**에서 할 일과 주기(매일·매주·매월)·시간을 정한다. 이름·시간대·실행 방식·재시도·텔레그램 알림은 **이름과 추가 설정**에 있다.
2. **자동화 저장**은 예약만 저장한다. 바로 돌리려면 **지금 실행**을 누른다.
3. 자동화를 고르면 최근 실행 결과가 보인다. 이전 실행과 원본 기록은 접힌 영역에 있다.
4. **예약 끄기**는 다음 예약을 막는다. 이미 시작한 실행은 취소하지 않는다.

텔레그램 알림은 기본으로 꺼져 있다. 정의는 `~/.omnux/routines.json`, 실행 결과는 `workspace/coding/routines/`에 남는다.

## 탐색

탭: 웹 찾기 · 브라우저 · 캔버스 · 기록

웹 검색과 URL 가져오기, 브라우저 제어, 캔버스 표시를 한 화면에서 한다. 브라우저는 새 임시 컨텍스트에서 setup이 설치한 Playwright Chromium을 쓰고, 없으면 설치된 Chrome을 쓴다. 실행할 수 없으면 오류를 반환한다.

## 리뷰

Safe Refactor 화면이다. 탭 순서대로 1. 파일 → 2. 바꿀 내용 → 3. 확인·적용을 진행한다. 적용 직전에 파일 상태를 다시 확인한다. 자세한 내용은 [Safe Refactor](./SAFE_REFACTORING.md)에 있다.

## 프로젝트 · 작업 · 노트

| 화면 | 탭 | 하는 일 |
|---|---|---|
| 프로젝트 | 목록 · 추가(수정) | 로컬 프로젝트 등록과 관리 (`~/.omnux/projects.json`) |
| 작업 | 목록 · 새 계획 · 계획 · 실행 · 결과 | 계획 생성, 리뷰, 승인, 실행 |
| 노트 | 남기기 · 결정 · 확인 · 메모 · 이어보기 | 결정과 검증 기록, 다음 세션용 handoff |

작업 화면의 계획·실행·결과 탭은 해당 상태가 있을 때만 나타난다.

## 엔진

| 화면 | 탭 |
|---|---|
| 에이전트 | 실행 중 · 흐름 · 공유 기록 · 작업 폴더 · 기록 남기기 |
| 도구 | 전체 · 프로젝트 · 전역 (`.omni/skills/`, `~/.omnux/skills/`) |
| 확장 | 훅 · 승인 · 규칙 · 플러그인 |
| 라우팅 | 작업별 경로 · 최근 선택 · 로컬 모델 |
| 규칙 | 목록 · 그림 · 속성 · 실행 |

## 모니터

| 화면 | 탭 |
|---|---|
| 활동 | 이 세션 기록 · 세션 타임라인 |
| 로그 | 모델 호출 · 진단 |
| 상태 | 연결 · 점검 · 작업 · 도구 |

상태 화면의 **점검 > 환경 진단**에서 Doctor 결과와 복구를, **도구 > 정리**에서 산출물 정리를 한다.

## 설정

탭: 일반 · 모델 · 보안 · 연동 · 데이터 · 정보

| 탭 | 들어 있는 것 |
|---|---|
| 일반 | 앱 표시, 시작 시 실행, 단축키, 음성 출력, 기본 프로젝트, 사용자 규칙 |
| 모델 | 모델 선택, API 키, CLI 연결, 우선순위, 사용량 |
| 보안 | 앱 인증, 외부 접속, 권한 |
| 연동 | Telegram, 클라우드 동기화 |
| 데이터 | 메모리 노트, 백업 |
| 정보 | 연결 상태, 앱 정보 |

## 테마

Light(기본), Glass, Dark 세 가지다. 상단 테마 버튼으로 바꾼다.
